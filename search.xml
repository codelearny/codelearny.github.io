<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>AbstractQueuedSynchronizer</title>
    <url>/2021/06/22/AbstractQueuedSynchronizer/</url>
    <content><![CDATA[]]></content>
      <tags>
        <tag>JUC</tag>
        <tag>AQS</tag>
      </tags>
  </entry>
  <entry>
    <title>Dockerfile常用指令</title>
    <url>/2021/03/18/Dockerfile%E5%B8%B8%E7%94%A8%E6%8C%87%E4%BB%A4/</url>
    <content><![CDATA[<blockquote>
<p>现在，就是开始行动的最佳时机</p>
</blockquote>
<h4 id="CMD"><a href="#CMD" class="headerlink" title="CMD"></a>CMD</h4><p>  指定一个容器启动时要运行的命令，可以指定参数，最好将命令放在一个数组结构中 CMD [“/bin/bash”,”true”]</p>
<h4 id="ENTRYPOINT"><a href="#ENTRYPOINT" class="headerlink" title="ENTRYPOINT"></a>ENTRYPOINT</h4><p>  与CMD类似，但是CMD会被run命令覆盖，而run命令的参数会被当作参数再次传递给ENTRYPOINT</p>
<h4 id="WORKDIR"><a href="#WORKDIR" class="headerlink" title="WORKDIR"></a>WORKDIR</h4><p>  设置工作目录，可多次设置</p>
<h4 id="ENV"><a href="#ENV" class="headerlink" title="ENV"></a>ENV</h4><p>  设置环境变量，可在其他指令中使用$ENV引用，也可以在run命令中使用-e参数传递环境变量，这些变量只在运行时有效</p>
<h4 id="USER"><a href="#USER" class="headerlink" title="USER"></a>USER</h4><p>  指定运行容器的用户，uid或组及其组合，不指定默认为root</p>
<h4 id="VOLUME"><a href="#VOLUME" class="headerlink" title="VOLUME"></a>VOLUME</h4><p>  添加卷，也可以通过-v参数指定挂载卷 -v 本地目录：容器目录</p>
<h4 id="EXPOSE"><a href="#EXPOSE" class="headerlink" title="EXPOSE"></a>EXPOSE</h4><p>  暴露的端口号，在启动容器时需要通过-P（注意是大写），Docker主机会自动分配一个端口转发到指定的端口；使用-p，则可以具体指定哪个本地端口映射过来。</p>
<h4 id="ADD"><a href="#ADD" class="headerlink" title="ADD"></a>ADD</h4><p>  用于添加环境中的文件或目录到容器中，ADD src dest/path,src可以是url或文件或目录</p>
<h4 id="COPY"><a href="#COPY" class="headerlink" title="COPY"></a>COPY</h4><p>  类似ADD,但是只是复制而不会提取和解压</p>
<h4 id="ONBUILD"><a href="#ONBUILD" class="headerlink" title="ONBUILD"></a>ONBUILD</h4><p>  为镜像添加触发器，当一个镜像被用作其他镜像的基础镜像时，该触发器会执行，注意只能被继承一次</p>
]]></content>
      <categories>
        <category>容器</category>
        <category>Docker</category>
      </categories>
      <tags>
        <tag>Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker常用命令</title>
    <url>/2021/03/17/Docker%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4/</url>
    <content><![CDATA[<h2 id="容器"><a href="#容器" class="headerlink" title="容器"></a>容器</h2><p>容器的基本信息</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ docker info</span><br></pre></td></tr></table></figure>
<p>查看版本信息</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ docker version</span><br></pre></td></tr></table></figure>
<p>创建容器，并打开bash交互</p>
<blockquote>
<p>-i 开启stdin， -t 分配一个伪tty终端，-d 指定后台运行， –name 指定容器名称， -p &lt;主机端口&gt;:&lt;容器端口&gt; 指定端口，-v &lt;主机目录&gt;:&lt;容器目录&gt; 挂载卷 ， –link &lt;目标容器名&gt;:&lt;别名&gt; 连接容器 ，–privileged 特权模式 </p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ docker run -i -t &lt;image_name/continar_id&gt; /bin/bash  </span><br></pre></td></tr></table></figure>
<p>附着到正在运行的容器</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ docker attach &lt;id/container_name&gt;  </span><br></pre></td></tr></table></figure>
<p>在容器内部启动新进程</p>
<blockquote>
<p>-it 交互式进程 -d 后台进程（docker exec -d <af7as3df> touch /etc/myconf）</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ docker <span class="built_in">exec</span> -t -i &lt;id/container_name&gt;  /bin/bash  </span><br><span class="line">```    </span><br><span class="line">查看容器日志</span><br><span class="line">&gt;-f 监控 (类似 tail -f) ，-t 显示时间戳</span><br><span class="line">```bash</span><br><span class="line">$ docker logs &lt;id/container_name&gt;  </span><br><span class="line">```    </span><br><span class="line">列出当前所有正在运行的容器</span><br><span class="line">&gt;-a  列出所有容器， -l 最后一次运行的容器</span><br><span class="line">```bash</span><br><span class="line">$ docker ps  </span><br><span class="line">```    </span><br><span class="line">显示一个运行的容器里面的进程信息</span><br><span class="line">```bash</span><br><span class="line">$ docker top &lt;id/container_name&gt;  </span><br><span class="line">```    </span><br><span class="line">查看容器内部详情细节</span><br><span class="line">&gt;$ docker inspect -f <span class="string">&#x27;&amp;#123;&amp;#123; .NetworkSettings.IPAddress &amp;#125;&amp;#125;&#x27;</span> &lt;id/container_name&gt;</span><br><span class="line">```bash</span><br><span class="line">$ docker inspect &lt;id/container_name&gt;  </span><br><span class="line">```    </span><br><span class="line">从容器里面拷贝文件/目录到本地一个路径</span><br><span class="line">```bash</span><br><span class="line">$ docker cp &lt;id/container_name&gt;:/container_path to_path  </span><br></pre></td></tr></table></figure>
<p>启动、停止(SIGTERM信号)、重启、杀掉(SIGKILL信号)、删除单个容器</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ docker start/stop/restart/<span class="built_in">kill</span>/rm &lt;id/container_name&gt;  </span><br></pre></td></tr></table></figure>
<h2 id="镜像"><a href="#镜像" class="headerlink" title="镜像"></a>镜像</h2><p>列出所有镜像，可指定镜像名称</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ docker images  </span><br></pre></td></tr></table></figure>
<p>从dockerHub检索镜像</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ docker search &lt;image_name&gt;  </span><br></pre></td></tr></table></figure>
<p>拉取镜像</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ docker pull &lt;image_name&gt;  </span><br></pre></td></tr></table></figure>
<p>删除镜像</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ docker rmi &lt;image_name&gt;  </span><br></pre></td></tr></table></figure>
<p>显示镜像历史，可以看到镜像的每一层以及创建这些层的指令</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ docker <span class="built_in">history</span> &lt;image_name&gt;  </span><br></pre></td></tr></table></figure>
<p>发布镜像到仓库</p>
<blockquote>
<p>私有仓库需要加上域名端口（docker push dockerhub.yourdomain.<br>  com:443/hello.demo.kdemo:v1.0）</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ docker push &lt;new_image_name&gt;  </span><br></pre></td></tr></table></figure>
<p>根据Dockerfile构建镜像</p>
<blockquote>
<p>-t 指定仓库/镜像名:标签， . 代表当前目录的Dockerfile，也可以指定git仓库地址</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ docker build -t <span class="string">&#x27;&lt;repo&gt;/&lt;image_name&gt;:&lt;tag&gt;&#x27;</span> .</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>容器</category>
        <category>Docker</category>
      </categories>
      <tags>
        <tag>Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>JWT简介</title>
    <url>/2021/04/24/JWT%E7%AE%80%E4%BB%8B/</url>
    <content><![CDATA[<h1 id="JSON-Web-Token"><a href="#JSON-Web-Token" class="headerlink" title="JSON Web Token"></a>JSON Web Token</h1><blockquote>
<p><a href="https://jwt.io/introduction">官网说明</a></p>
</blockquote>
<h2 id="What"><a href="#What" class="headerlink" title="What"></a>What</h2><p><code>JSON Web Token</code>（<code>JWT</code>）是一个开放标准（<a href="https://tools.ietf.org/html/rfc7519">RFC 7519</a>），它定义了一种紧凑的、自包含的方式，可以将各方之间的信息作为JSON对象安全地传输。<br>基于数字签名进行验证，可以使用<code>HMAC</code>算法或者基于公钥/私钥对的<code>RSA</code>或<code>ECDSA</code>进行签名</p>
<h3 id="structure"><a href="#structure" class="headerlink" title="structure"></a>structure</h3><p><code>JWT</code>由三部分构成，由点号（<code>.</code>）分割</p>
<ul>
<li>Header</li>
<li>Payload</li>
<li>Signature</li>
</ul>
<p>通常由以上三部分组成如下的形式</p>
<p><code>xxxx.yyyy.zzzz</code></p>
<h4 id="Header"><a href="#Header" class="headerlink" title="Header"></a>Header</h4><p><code>Header</code>通常由两部分组成：令牌的类型（<code>JWT</code>）和使用的签名算法（例如 <code>HMAC SHA256</code> 或 <code>RSA</code>）</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="attr">&quot;alg&quot;</span>: <span class="string">&quot;HS256&quot;</span>,</span><br><span class="line">  <span class="attr">&quot;typ&quot;</span>: <span class="string">&quot;JWT&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>然后对这个<code>JSON</code>进行<code>Base64Url</code>编码形成<code>JWT</code>的第一部分</p>
<h4 id="Payload"><a href="#Payload" class="headerlink" title="Payload"></a>Payload</h4><p><code>Payload</code>包含声明（<code>claims</code>）。声明是关于实体（通常是用户）和附加数据的声明。<br>声明有三种类型：<em>registered</em>,<em>public</em>,<em>private</em></p>
<ul>
<li><p><strong>Registered claims</strong><br>这些是一组预定义的声明，它们不是强制性的，而是推荐的，以提供一组有用的、可互操作的声明。<br>其中一些是：<strong>iss</strong>（发行者）、<strong>exp</strong>（到期时间）、<strong>sub</strong>（主题）、<strong>aud</strong>（受众）和其他。</p>
</li>
<li><p><strong>Public claims</strong><br>这些可以由使用<code>JWT</code>的人随意定义。但是为了避免冲突，应该在<a href="https://www.iana.org/assignments/jwt/jwt.xhtml"> IANA JSON Web Token Registry </a>中定义它们，或者将它们定义为包含防冲突命名空间的URI。</p>
</li>
<li><p><strong>Private claims</strong><br>这些是为在同意使用它们的各方之间共享信息而创建的自定义声明，既不是<em>Registered</em>，也不是<em>Public</em>。</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="attr">&quot;sub&quot;</span>: <span class="string">&quot;1234567890&quot;</span>,</span><br><span class="line">  <span class="attr">&quot;name&quot;</span>: <span class="string">&quot;John Doe&quot;</span>,</span><br><span class="line">  <span class="attr">&quot;admin&quot;</span>: <span class="literal">true</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>然后对这个<code>JSON</code>进行<code>Base64Url</code>编码形成<code>JWT</code>的第二部分</p>
<blockquote>
<p>请注意，对于已签名的令牌，此信息虽然受到了防篡改保护，但任何人都可以读取。不要将敏感信息放在<code>JWT</code>的有<code>Payload</code>或<code>Header</code>元素中，除非它是加密的。</p>
</blockquote>
</li>
</ul>
<h4 id="Signature"><a href="#Signature" class="headerlink" title="Signature"></a>Signature</h4><p>签名部分由编码的<code>Header</code>、编码的<code>Payload</code>、一个密钥、报头中指定的算法，并对其进行签名。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">HMACSHA256(</span><br><span class="line">  base64UrlEncode(header) + &quot;.&quot; +</span><br><span class="line">  base64UrlEncode(payload),</span><br><span class="line">  secret)</span><br></pre></td></tr></table></figure>
<p>签名用于验证消息在发送过程中没有发生更改，对于使用私钥签名的令牌，它还可以验证<code>JWT</code>的发送者是它所说的发送者。</p>
<h4 id="Encoded"><a href="#Encoded" class="headerlink" title="Encoded"></a>Encoded</h4><p>将以上三部分<code>Base64-URL</code>编码的字符串以点号分隔，就得到了最终的令牌</p>
<p><code>eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IkpvaG4gRG9lIiwiaWF0IjoxNTE2MjM5MDIyfQ.SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c</code></p>
<p>可以使用<a href="https://jwt.io/#debugger-io"> jwt.io Debugger </a>进行解码，验证，生成<code>JWT</code></p>
<h2 id="When"><a href="#When" class="headerlink" title="When"></a>When</h2><h3 id="Authorization"><a href="#Authorization" class="headerlink" title="Authorization"></a>Authorization</h3><p>这是使用<code>JWT</code>最常见的场景。<br>一旦用户登录，每个后续请求都将包括<code>JWT</code>，允许用户访问该令牌允许的路由、服务和资源。<br>单点登录是目前广泛使用JWT的一个特性，因为它的开销很小，并且能够很容易地跨不同的域使用。</p>
<h3 id="Information-Exchange"><a href="#Information-Exchange" class="headerlink" title="Information Exchange"></a>Information Exchange</h3><p><code>JWT</code>是在各方之间安全地传输信息的一种好方法。<br>因为<code>JWT</code>可以签名，例如使用公钥/私钥对，您可以确保发送者的身份。<br>此外，由于签名是使用<code>Hader</code>和<code>Payload</code>计算的，因此您还可以验证内容没有被篡改。</p>
<h2 id="How"><a href="#How" class="headerlink" title="How"></a>How</h2><p>在身份验证中，当用户使用其凭据成功登录时，将返回一个<code>JWT</code>。因为令牌是凭证，所以必须非常小心地防止安全问题。通常，您不应该将令牌保留的时间超过所需的时间。</p>
<p>每当用户想要访问受保护的路由或资源时，用户代理都应该发送JWT，通常在<code>Authorization header</code>中使用<code>Bearer schema</code>。标题的内容应如下所示：</p>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line">Authorization: Bearer &lt;token&gt;</span><br></pre></td></tr></table></figure>
<p>在某些情况下，这可以是无状态授权机制。服务器的受保护路由将在<code>Authorization header</code>中检查有效的<code>JWT</code>，如果存在，则允许用户访问受保护的资源。如果<code>JWT</code>包含必要的数据，则可以在一些情况下减少查询数据库的操作。</p>
<p>如果令牌是在<code>Authorization header</code>中发送的，不会存在跨源资源共享（<code>CORS</code>）问题，因为它不使用<code>cookies</code>。</p>
<p>通常的流程如下：</p>
<ol>
<li>应用程序或客户端向授权服务器请求授权。这是通过不同的授权流之一执行的。例如，一个典型的<a href="https://openid.net/connect/"> OpenID Connect </a>兼容web应用程序将通过<code>/oauth/authorize</code>端点使用<a href="https://openid.net/specs/openid-connect-core-1_0.html#CodeFlowAuth"> authorization code flow</a>。</li>
<li>当授权通过时，授权服务器向应用程序返回一个访问令牌。</li>
<li>应用程序使用访问令牌访问受保护的资源（如<code>API</code>）。<blockquote>
<p>请注意，对于签名令牌，令牌中包含的所有信息都会暴露给用户或其他方，即使他们无法更改它。这意味着您不应该在令牌中放置机密信息。</p>
</blockquote>
</li>
</ol>
<h2 id="Why"><a href="#Why" class="headerlink" title="Why"></a>Why</h2><p>让我们讨论一下<code>JSON Web Token</code>（<code>JWT</code>）与<code>Simple Web Tokens</code>（<code>SWT</code>）和<code>Security Assertion Markup Language Tokens</code>（<code>SAML</code>）相比的优势。</p>
<p>由于<code>JSON</code>没有<code>XML</code>那么冗长，因此当它被编码时，它的大小也更小，这使得<code>JWT</code>比<code>SAML</code>更紧凑。这使得<code>JWT</code>成为在<code>HTML</code>和<code>HTTP</code>环境中传递的好选择。</p>
<p>安全方面，<code>SWT</code>只能使用<code>HMAC</code>算法由共享密钥对称签名。但是，<code>JWT</code>和<code>SAML</code>令牌可以使用<code>X.509</code>证书形式的公钥/私钥对进行签名。与签名<code>JSON</code>的简单性相比，使用<code>XML</code>数字签名来签名<code>XML</code>而不引入模糊的安全漏洞是非常困难的。</p>
<p><code>JSON</code>解析器在大多数编程语言中都很常见，因为它们直接映射到对象。相反，<code>XML</code>没有自然的文档到对象的映射。这使得使用<code>JWT</code>比使用<code>SAML</code>断言更容易。</p>
<p>关于使用，<code>JWT</code>是在互联网规模上使用的。这突出了<code>JWT</code>在多个平台（尤其是移动平台）上客户端处理的方便性。</p>
<p>更多信息<a href="http://auth0.com/learn/json-web-tokens?_ga=2.193042795.793010448.1619229222-2061826501.1619229222">Auth0</a></p>
]]></content>
      <categories>
        <category>Java</category>
        <category>Spring</category>
        <category>Spring Security</category>
        <category>Web</category>
        <category>JWT</category>
      </categories>
      <tags>
        <tag>JWT</tag>
        <tag>secure</tag>
      </tags>
  </entry>
  <entry>
    <title>线上进程问题排查常用方法</title>
    <url>/2021/03/08/Java-error-check/</url>
    <content><![CDATA[<h3 id="CPU"><a href="#CPU" class="headerlink" title="CPU"></a>CPU</h3><p>查看进程pid</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> ps -ef|grep xxxService</span></span><br></pre></td></tr></table></figure>
<p>查看cpu使用率高的线程</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> top -H -p pid</span> </span><br></pre></td></tr></table></figure>
<p>得到线程id nid  </p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> <span class="built_in">printf</span> <span class="string">&#x27;%x\n&#x27;</span> pid</span> </span><br></pre></td></tr></table></figure>
<p>查看java线程堆栈信息</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> jstack pid|grep nid -C10 -color</span>  </span><br></pre></td></tr></table></figure>
<h3 id="内核调用"><a href="#内核调用" class="headerlink" title="内核调用"></a>内核调用</h3><p>跟踪进程系统调用</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> strace -cp pid</span></span><br></pre></td></tr></table></figure>
<p>跟踪进程用户态运行时栈</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> pstack pid</span></span><br></pre></td></tr></table></figure>
<p>ps命令查看进程的运行状态，S列，阻塞函数，WCHAN列，man ps 可查询状态的详细说明</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> ps -lfp pid</span></span><br><span class="line">F S UID        PID  PPID  C PRI  NI ADDR SZ WCHAN  STIME TTY          TIME CMD</span><br><span class="line">4 S root         9     0  0  80   0 -  1027 do_wai 02:38 pts/1    00:00:00 /bin/bash</span><br></pre></td></tr></table></figure>
<p>显示当进程sleep时，kernel当前运行的函数</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> cat /proc/&lt;pid&gt;/wchan</span></span><br><span class="line">do_wait</span><br></pre></td></tr></table></figure>
<p>当前进程正在进行的系统调用，第一个数字代表系统调用号，参考内核源码，如include/asm/unistd.h，后面跟着系统调用的参数值（位于寄存器中），最后两个值是堆栈指针和指令计数器，如果当前进程的阻塞不是系统调用产生，则系统调用号的值为-1。如果进程没有阻塞，则文件只有一个<code>running</code>的字符串</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> cat /proc/&lt;pic&gt;/syscall</span></span><br><span class="line">61 0xffffffff 0x7ffd255ae8a0 0xa 0x0 0x0 0x7 0x7ffd255ae888 0x7f6d6c283dba</span><br></pre></td></tr></table></figure>
<p>当前进程的内核调用栈信息</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> cat /proc/&lt;pid&gt;/stack</span></span><br><span class="line">[&lt;ffffffff80168375&gt;] poll_schedule_timeout+0x45/0x60</span><br><span class="line">[&lt;ffffffff8016994d&gt;] do_sys_poll+0x49d/0x550</span><br><span class="line">[&lt;ffffffff80169abd&gt;] SyS_poll+0x5d/0xf0</span><br><span class="line">[&lt;ffffffff804c16e7&gt;] system_call_fastpath+0x16/0x1b</span><br><span class="line">[&lt;00007f4a41ff2c1d&gt;] 0x7f4a41ff2c1d</span><br><span class="line">[&lt;ffffffffffffffff&gt;] 0xffffffffffffffff</span><br></pre></td></tr></table></figure>
<p>当前进程打开的文件，目录中的每一项都是一个符号链接，指向打开的文件，数字则代表文件描述符。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> ls -lt /proc/&lt;pid&gt;/fd</span></span><br><span class="line">total 0</span><br><span class="line">lrwx------ 1 root root 64 May 29 03:23 255 -&gt; /dev/pts/1</span><br><span class="line">lrwx------ 1 root root 64 May 29 02:38 0 -&gt; /dev/pts/1</span><br><span class="line">lrwx------ 1 root root 64 May 29 02:38 1 -&gt; /dev/pts/1</span><br><span class="line">lrwx------ 1 root root 64 May 29 02:38 2 -&gt; /dev/pts/1</span><br></pre></td></tr></table></figure>
<p>根据进程id确认当前运行进程的路径</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> netstat -nltp |grep 8888</span></span><br><span class="line">(Not all processes could be identified, non-owned process info</span><br><span class="line"> will not be shown, you would have to be root to see it all.)</span><br><span class="line">tcp        0      0 0.0.0.0:8888                0.0.0.0:*                   LISTEN      11019/nginx</span><br><span class="line"><span class="meta">$</span><span class="bash"> ll /proc/11019/exe</span></span><br><span class="line">lrwxrwxrwx 1 was was 0 Apr 23 10:12 /proc/11019/exe -&gt; /was/server/nginx/sbin/nginx</span><br></pre></td></tr></table></figure>

<h3 id="GC"><a href="#GC" class="headerlink" title="GC"></a>GC</h3><p>查看进程gc信息（采样间隔1000ms）</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> jstat -gc pid 1000</span></span><br></pre></td></tr></table></figure>
<h3 id="线程"><a href="#线程" class="headerlink" title="线程"></a>线程</h3><p>线程数统计  </p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> pstree -p pid | wc -l</span></span><br><span class="line"><span class="meta">$</span><span class="bash"> ls -l /proc/pid/task | wc -l</span></span><br></pre></td></tr></table></figure>
<h3 id="上下文切换"><a href="#上下文切换" class="headerlink" title="上下文切换"></a>上下文切换</h3><p>操作系统上下文切换，采样间隔1s，采样次数10次</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> vmstat 1 10</span></span><br></pre></td></tr></table></figure>
<h3 id="内存"><a href="#内存" class="headerlink" title="内存"></a>内存</h3><p>内存整体使用情况  </p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> free</span> </span><br></pre></td></tr></table></figure>
<p>导出dump文件，可视化工具分析</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> jmap -dump:format=b,file=&#123;filename&#125; &#123;pid&#125;</span></span><br></pre></td></tr></table></figure>
<p>内存段前三十,隔一段时间再跑一次对比  </p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> pmap -x pid | sort -rn -k3 | head -30</span></span><br></pre></td></tr></table></figure>
<p>如果有可疑内存段需要分析  </p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> gdb --batch --pid &#123;pid&#125; -ex <span class="string">&quot;dump memory &#123;filename&#125;.dump &#123;内存起始地址&#125; &#123;内存起始地址&#125;+&#123;内存块大小&#125;&quot;</span></span>   </span><br></pre></td></tr></table></figure>
<p>NMT是Java7U40引入的HotSpot新特性，配合jcmd命令我们就可以看到具体内存组成了。</p>
<blockquote>
<p>需要在启动参数中加入 -XX:NativeMemoryTracking=summary 或者 -XX:NativeMemoryTracking=detail，会有略微性能损耗。  </p>
</blockquote>
<p>一般对于堆外内存缓慢增长直到爆炸的情况来说，可以先设一个基线  </p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> jcmd &#123;pid&#125; VM.native_memory baseline</span>  </span><br></pre></td></tr></table></figure>
<p> 然后等放一段时间后再去看看内存增长的情况，通过  </p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> jcmd &#123;pid&#125; VM.native_memory summary.diff</span>  </span><br><span class="line"><span class="meta">$</span><span class="bash"> jcmd &#123;pid&#125; VM.native_memory detail.diff</span>  </span><br></pre></td></tr></table></figure>
<p> 做一下summary或者detail级别的diff。<br> 系统层面，我们还可以使用strace命令来监控内存分配  </p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> strace -f -e <span class="string">&quot;brk,mmap,munmap&quot;</span> -p &#123;pid&#125;</span></span><br></pre></td></tr></table></figure>
<h3 id="磁盘"><a href="#磁盘" class="headerlink" title="磁盘"></a>磁盘</h3><p>查看磁盘空间占用情况</p>
<blockquote>
<p>-h 方便阅读显示，-l 只显示本地文件系统</p>
</blockquote>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> df -hl</span></span><br></pre></td></tr></table></figure>
<h3 id="IO"><a href="#IO" class="headerlink" title="IO"></a>IO</h3><p>监控系统设备的IO负载情况</p>
<blockquote>
<p>-d 显示设备（磁盘）使用状态；-k 某些使用block为单位的列强制使用Kilobytes为单位；1表示数据显示每隔1秒刷新一次。10表示刷新10次</p>
</blockquote>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> iostat -d -x -k 1 10</span>  </span><br></pre></td></tr></table></figure>
<p>查看io源</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> iotop -o</span></span><br></pre></td></tr></table></figure>
<p>查看指定的命令正在使用的文件和网络连接  </p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> lsof -c &#123;file&#125;</span> </span><br></pre></td></tr></table></figure>
<p>查看指定进程ID已打开的内容  </p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> lsof -p &#123;pid&#125;</span></span><br></pre></td></tr></table></figure>
<p>显示所有连接  </p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> lsof -i</span></span><br></pre></td></tr></table></figure>
<p>显示与指定端口相关的网络信息  </p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> lsof -i:&#123;port&#125;</span></span><br></pre></td></tr></table></figure>
<p>查看进程使用端口相关的网络信息  </p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> netstat -nltp</span></span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>jstack</tag>
        <tag>jmap</tag>
        <tag>ps</tag>
      </tags>
  </entry>
  <entry>
    <title>MarkDown 基本语法</title>
    <url>/2021/01/09/Play/</url>
    <content><![CDATA[<h3 id="标题"><a href="#标题" class="headerlink" title="标题"></a>标题</h3><pre><code>大标题
===
小标题
---
# 一级标题
## 二级标题
### 三级标题
#### 四级标题
##### 五级标题
###### 六级标题</code></pre>
<h3 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h3><pre><code>&gt;this is a block
&gt;&gt;this is another
&gt;&gt;&gt;段落的换行两个空格加回车  
或者直接用空行分开</code></pre>
<blockquote>
<p>this is a block</p>
<blockquote>
<p>this is another</p>
<blockquote>
<p>段落的换行两个空格加回车<br>或者直接用空行分开</p>
</blockquote>
</blockquote>
</blockquote>
<h3 id="列表"><a href="#列表" class="headerlink" title="列表"></a>列表</h3><pre><code>1. 有序列表只需要1开头
20. 其他数字不影响顺序
13. cccc
* 无序列表
    + 列表嵌套只需要加缩进
        - ffff</code></pre>
<ol>
<li>有序列表只需要1开头</li>
<li>其他数字不影响顺序</li>
<li>cccc</li>
</ol>
<ul>
<li>无序列表<ul>
<li>列表嵌套只需要加缩进<ul>
<li>ffff</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="代码块"><a href="#代码块" class="headerlink" title="代码块"></a>代码块</h3><p>缩进四个空格表示代码块</p>
<pre><code>    //代码块</code></pre>
<p>三个`表示代码块，js为代码语言</p>
<pre>
<code>```js</code>
console.log('hello')
<code>```</code>
</pre>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(<span class="string">&#x27;hello&#x27;</span>)</span><br></pre></td></tr></table></figure>
<h3 id="分割线"><a href="#分割线" class="headerlink" title="分割线"></a>分割线</h3><pre><code>***</code></pre>
<hr>
<pre><code>---</code></pre>
<hr>
<h3 id="字体效果"><a href="#字体效果" class="headerlink" title="字体效果"></a>字体效果</h3><pre><code>*斜体*  _斜体_</code></pre>
<p><em>斜体</em>  <em>斜体</em></p>
<pre><code>**粗体**  __粗体__</code></pre>
<p><strong>粗体</strong>  <strong>粗体</strong></p>
<pre><code>***粗体+斜体***</code></pre>
<p><strong><em>粗体+斜体</em></strong></p>
<pre><code>~~删除线~~</code></pre>
<p><del>删除线</del></p>
<pre><code>`阴影`</code></pre>
<p><code>阴影</code></p>
<h3 id="表格"><a href="#表格" class="headerlink" title="表格"></a>表格</h3><pre><code>name|age|tall|address
--|:--:|:--|--:
默认|居中|左对齐|右对齐
black|22|188|a</code></pre>
<table>
<thead>
<tr>
<th>name</th>
<th align="center">age</th>
<th align="left">tall</th>
<th align="right">address</th>
</tr>
</thead>
<tbody><tr>
<td>默认</td>
<td align="center">居中</td>
<td align="left">左对齐</td>
<td align="right">右对齐</td>
</tr>
<tr>
<td>black</td>
<td align="center">22</td>
<td align="left">188</td>
<td align="right">a</td>
</tr>
</tbody></table>
<h3 id="图片与链接"><a href="#图片与链接" class="headerlink" title="图片与链接"></a>图片与链接</h3><h4 id="链接"><a href="#链接" class="headerlink" title="链接"></a>链接</h4><p>链接定义 <code>\[内容\](地址 &quot;标题&quot;)</code><br>也可以直接用<code>&lt;地址&gt;</code></p>
<pre><code>[example](https://codelearny.github.io/ &quot;the description of this site&quot;) with description.
[example](https://codelearny.github.io/) without title attribute.
&lt;https://codelearny.github.io/&gt;</code></pre>
<p><a href="https://codelearny.github.io/" title="the description of this site">example</a> with description.<br><a href="https://codelearny.github.io/">example</a> without title attribute.<br><a href="https://codelearny.github.io/">https://codelearny.github.io/</a></p>
<p>可以将链接地址设为变量<code>[标识]:地址 &quot;标题&quot;</code><br>定义时直接引用<code>[内容][标识]</code></p>
<pre><code>[1]:https://github.com
[my blog]:https://codelearny.github.io/ &quot;我的博客&quot;
[This is my blog][my blog]
[github][1]</code></pre>
<p><a href="https://codelearny.github.io/" title="我的博客">This is my blog</a><br><a href="https://github.com/">github</a></p>
<h4 id="图片"><a href="#图片" class="headerlink" title="图片"></a>图片</h4><p>图片定义和链接类似，以感叹号开头<code>![Alt Text](地址 &quot;标题&quot;)</code><br>也可以使用变量<code>![Alt Text][标识])</code></p>
<pre><code>![说明](https://github.githubassets.com/favicons/favicon.png &quot;提示文字&quot;)
![一个图片][2]
[2]:/path/to/pic</code></pre>
<p><img src="https://github.githubassets.com/favicons/favicon.png" alt="说明" title="提示文字"></p>
<h4 id="锚点"><a href="#锚点" class="headerlink" title="锚点"></a>锚点</h4><p>锚点定义和链接定义类似 <code>[内容](#目标)</code><br>跳转的目标可以是各级标题<br>也可以使用html标记的id属性<code>&lt;span id=&quot;id&quot;&gt;锚点目标&lt;/span&gt;</code><br><code>[内容](#id)</code></p>
<pre><code>[跳转到标题](#标题)
[跳转到列表](#列表)</code></pre>
<p><a href="#%E6%A0%87%E9%A2%98">跳转到标题</a><br><a href="#%E5%88%97%E8%A1%A8">跳转到列表</a></p>
<h3 id="其它"><a href="#其它" class="headerlink" title="其它"></a>其它</h3><p>Markdown支持html标签，可以直接在文档中使用</p>
<pre><code>&lt;kbd&gt;kbd&lt;/kbd&gt;</code></pre>
<p><kbd>kbd</kbd><br>Markdown使用\转义特殊字符</p>
<pre><code>\`转义\`</code></pre>
<p>`转义`</p>
]]></content>
      <categories>
        <category>Java</category>
        <category>DB</category>
        <category>Spring</category>
        <category>Mysql</category>
        <category>Spring Boot</category>
        <category>Spring Cloud</category>
        <category>JS</category>
        <category>Node</category>
        <category>VUE</category>
        <category>React</category>
        <category>Network</category>
        <category>HTTP</category>
        <category>TCP/IP</category>
        <category>Python</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>C++</tag>
        <tag>Python</tag>
        <tag>JS</tag>
        <tag>Mysql</tag>
      </tags>
  </entry>
  <entry>
    <title>Secure</title>
    <url>/2021/01/16/Secure/</url>
    <content><![CDATA[<h1 id="网络安全–加密相关"><a href="#网络安全–加密相关" class="headerlink" title="网络安全–加密相关"></a>网络安全–加密相关</h1><h3 id="常见算法种类"><a href="#常见算法种类" class="headerlink" title="常见算法种类"></a>常见算法种类</h3><h4 id="编码"><a href="#编码" class="headerlink" title="编码"></a>编码</h4><blockquote>
<p>对数据存储统一规则<br>  主要用途：</p>
</blockquote>
<ol>
<li>字符集编码</li>
<li>URL编码</li>
<li>Base64编码<h4 id="摘要-Hash-Digest"><a href="#摘要-Hash-Digest" class="headerlink" title="摘要 Hash Digest"></a>摘要 Hash Digest</h4><blockquote>
<p>对任意长度数据定长输出<br>主要用途：</p>
</blockquote>
</li>
<li>验证数据是否被篡改</li>
<li>hash表结构</li>
<li>存储密码<h4 id="对称加密算法"><a href="#对称加密算法" class="headerlink" title="对称加密算法"></a>对称加密算法</h4><blockquote>
<p>加密解密的key相同 主要用途</p>
</blockquote>
</li>
<li>加密传输数据<h4 id="非对称加密算法"><a href="#非对称加密算法" class="headerlink" title="非对称加密算法"></a>非对称加密算法</h4><blockquote>
<p>加密公钥，解密私钥  </p>
</blockquote>
</li>
</ol>
<p>  -&gt; https 加密传输对称加密密钥  </p>
<blockquote>
<p>加密私钥，解密公钥<br>  -&gt; 签名 –带有密钥的消息摘要 保证数据来源，验证是否篡改</p>
</blockquote>
<h4 id="数字证书"><a href="#数字证书" class="headerlink" title="数字证书"></a>数字证书</h4><blockquote>
<p>以上技术的综合应用</p>
</blockquote>
<ol>
<li>链式签名，根证书验证，防止中间人攻击</li>
<li>证书中包含使用者公钥，签名，算法等信息，经过CA机构的私钥加密，作为签名</li>
<li>使用CA机构的公钥解密签名，得到使用公钥</li>
</ol>
]]></content>
      <tags>
        <tag>secure</tag>
      </tags>
  </entry>
  <entry>
    <title>Kafka简单使用</title>
    <url>/2021/06/28/Kafka%E7%AE%80%E5%8D%95%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[<h2 id="Kafka官网介绍"><a href="#Kafka官网介绍" class="headerlink" title="Kafka官网介绍"></a>Kafka官网介绍</h2><blockquote>
<p><code>Apache Kafka</code> 是一个开源分布式事件流平台，被数千家公司用于高性能数据管道，流分析，数据集成和任务关键型应用</p>
</blockquote>
<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><h3 id="Broker"><a href="#Broker" class="headerlink" title="Broker"></a>Broker</h3><p><code>Kafka</code>作为一个集群，运行在一台或者多台服务器上。集群中的每个实例称为<code>Broker</code>。</p>
<h3 id="topic"><a href="#topic" class="headerlink" title="topic"></a>topic</h3><p><code>Kafka</code>通过<code>topic</code>对存储的流数据进行分类。每一个<code>topic</code>由一个或多个分区日志<code>partition</code>组成，多个<code>partition</code>均衡的分布在集群的<code>Broker</code>上。</p>
<h3 id="partition"><a href="#partition" class="headerlink" title="partition"></a>partition</h3><p>每个<code>partition</code>都是有序且顺序不可变的记录集，并且不断地追加到结构化的<code>commit.log</code>文件。<code>partition</code>使得<code>Kafka</code>具有横向扩展能力，分区日志可以分布在任意<code>Broker</code>上，为了使消息数据具有容错性和高可用性，每个<code>partition</code>都可以<code>replica</code>复制，在不同的<code>Broker</code>上进行备份，其中一个作为<code>leader</code>，其它的<code>replica</code>作为<code>follwer</code>，<code>leader</code>处理一切对<code>partition</code>的读写请求，而<code>follwer</code>只需被动的同步<code>leader</code>上的数据。当<code>leader</code>宕机了，从<code>follower</code>中选举一个成为新的<code>leader</code>。<code>leader</code>均衡的分布在集群的<code>Broker</code>上使得系统具有高可用性。</p>
<h3 id="offset"><a href="#offset" class="headerlink" title="offset"></a>offset</h3><p><code>partition</code>中的每一个记录都会分配一个id号来表示顺序，我们称之为<code>offset</code>，<code>offset</code>用来唯一的标识分区中每一条记录。</p>
<h3 id="Producer"><a href="#Producer" class="headerlink" title="Producer"></a>Producer</h3><p><code>Producer</code>是向<code>Kafka</code>写入数据的客户端应用，<code>Producer</code>需要指定写入的<code>topic</code>，也可以指定<code>partition</code>写入。</p>
<h3 id="Consumer"><a href="#Consumer" class="headerlink" title="Consumer"></a>Consumer</h3><p><code>Consumer</code>是从<code>Kafka</code>读数据的客户端应用，在每一个消费者中唯一保存的元数据是<code>offset</code>。<code>offset</code>由消费者所控制:通常在读取记录后，消费者会以线性的方式增加<code>offset</code>，但是实际上，由于这个位置由消费者控制，所以消费者可以采用任何顺序来消费记录。例如，一个消费者可以重置到一个旧的<code>offset</code>，从而重新处理过去的数据；也可以跳过最近的记录，从”现在”开始消费。</p>
<h3 id="group"><a href="#group" class="headerlink" title="group"></a>group</h3><p><code>Consumer</code>使用<code>group id</code>来标识所属的消费组，通过消费组来订阅感兴趣的<code>topic</code>，<code>Kafka</code>会将消息广播到所有的消费组，对于同一个消息组的<code>Consumer</code>，消息记录会负载平衡分发。通过指定<code>group id</code>可以很方便的实现队列模式和发布订阅模式。</p>
<h2 id="快速开始"><a href="#快速开始" class="headerlink" title="快速开始"></a>快速开始</h2><p>kafka目录bin下，有许多便捷脚本，可以快速执行一些任务，如果需要了解脚本如何使用，不带参数直接运行。</p>
<h3 id="启动ZooKeeper"><a href="#启动ZooKeeper" class="headerlink" title="启动ZooKeeper"></a>启动ZooKeeper</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ ./bin/zookeeper-server-start.sh config/zookeeper.properties</span><br></pre></td></tr></table></figure>
<h3 id="启动Kafka-broker"><a href="#启动Kafka-broker" class="headerlink" title="启动Kafka broker"></a>启动Kafka broker</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ ./bin/kafka-server-start.sh config/server.properties</span><br></pre></td></tr></table></figure>
<h3 id="创建topic"><a href="#创建topic" class="headerlink" title="创建topic"></a>创建topic</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ ./bin/kafka-topics.sh --create --topic aztopic --bootstrap-server localhost:9990</span><br></pre></td></tr></table></figure>
<h3 id="查看topic"><a href="#查看topic" class="headerlink" title="查看topic"></a>查看topic</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ ./bin/kafka-topics.sh --describe --topic aztopic --bootstrap-server localhost:9990</span><br></pre></td></tr></table></figure>
<h3 id="发布消息"><a href="#发布消息" class="headerlink" title="发布消息"></a>发布消息</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ ./bin/kafka-console-producer.sh --topic aztopic --bootstrap-server localhost:9990</span><br></pre></td></tr></table></figure>
<h3 id="订阅消息"><a href="#订阅消息" class="headerlink" title="订阅消息"></a>订阅消息</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ ./bin/kafka-console-consumer.sh --topic aztopic --bootstrap-server localhost:9990 --from-beginning</span><br></pre></td></tr></table></figure>
<h3 id="查看group"><a href="#查看group" class="headerlink" title="查看group"></a>查看group</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ ./bin/kafka-consumer-groups.sh --bootstrap-server localhost:9990 --all-groups --list</span><br></pre></td></tr></table></figure>
<h3 id="导出日志文件内容"><a href="#导出日志文件内容" class="headerlink" title="导出日志文件内容"></a>导出日志文件内容</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ ./bin/kafka-dump-log.sh --files /was/kafka/data/kafka/TOPIC_DISPATCH_PRO_OSS2-0/00000000000000576560.log --print-data-log </span><br></pre></td></tr></table></figure>
<h3 id="查看topic的offset"><a href="#查看topic的offset" class="headerlink" title="查看topic的offset"></a>查看topic的offset</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ ./bin/kafka-run-class.sh kafka.tools.GetOffsetShell --broker-list localhost:9990 --topic aztopic --time -1</span><br></pre></td></tr></table></figure>
<h2 id="部分属性说明"><a href="#部分属性说明" class="headerlink" title="部分属性说明"></a>部分属性说明</h2><p>config/server.properties Broker配置</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="meta">zookeeper.connect</span>=<span class="string">hostname1:port1,hostname2:port2,hostname3:port3/chroot/path	Zookeeper主机地址</span></span><br><span class="line"><span class="meta">num.partitions</span>=<span class="string">1	每个topic的默认日志分区数</span></span><br><span class="line"><span class="meta">default.replication.factor</span>=<span class="string">1	自动创建topic时的默认副本个数</span></span><br><span class="line"><span class="meta">log.dir</span>=<span class="string">/tmp/kafka-logs	保存日志数据的目录（对log.dirs属性的补充）</span></span><br><span class="line"><span class="meta">broker.id</span>=<span class="string">-1  用于服务的broker id。如果没设置，将生存一个唯一broker id。为了避免ZooKeeper生成的id和用户配置的broker id相冲突，生成的id将在reserved.broker.max.id的值基础上加1。</span></span><br><span class="line"><span class="meta">log.flush.interval.messages</span>=<span class="string">9223372036854775807	在将消息刷新到磁盘之前，在日志分区上累积的消息数量。</span></span><br><span class="line"><span class="meta">log.flush.interval.ms</span>	<span class="string">在刷新到磁盘之前，任何topic中的消息保留在内存中的最长时间（以毫秒为单位）。如果未设置，则使用log.flush.scheduler.interval.ms中的值。</span></span><br><span class="line"><span class="meta">log.retention.bytes</span>=<span class="string">-1	日志删除的大小阈值</span></span><br><span class="line"><span class="meta">log.retention.hours</span>=<span class="string">168	日志删除的时间阈值（小时为单位）</span></span><br><span class="line"><span class="meta">log.roll.hours</span>=<span class="string">168	新日志段轮转时间间隔（小时为单位），次要配置为log.roll.ms</span></span><br><span class="line"><span class="meta">log.segment.bytes</span>=<span class="string">1073741824	单个日志段文件最大大小</span></span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>Kafka</tag>
      </tags>
  </entry>
  <entry>
    <title>editorconfig</title>
    <url>/2021/06/15/editorconfig/</url>
    <content><![CDATA[<p><a href="https://editorconfig.org/">EditorConfig官网</a></p>
<blockquote>
<p><code>EditorConfig</code>有助于在不同的编辑器和IDE中为在同一项目上工作的多个开发人员保持一致的编码样式。<code>EditorConfig</code>项目由一个用于定义编码样式的文件格式和一组文本编辑器插件组成，这些插件使编辑器能够读取文件格式并遵循定义的样式。<code>EditorConfig</code>文件易于阅读，并且与版本控制系统配合良好。</p>
</blockquote>
<p><code>VS Code</code>安装对应插件<br>编写<code>.editorconfig</code>文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># top-most EditorConfig file</span></span><br><span class="line">root = <span class="literal">true</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Unix-style newlines with a newline ending every file</span></span><br><span class="line">[*]</span><br><span class="line">end_of_line = lf</span><br><span class="line">insert_final_newline = <span class="literal">true</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Matches multiple files with brace expansion notation</span></span><br><span class="line"><span class="comment"># Set default charset</span></span><br><span class="line">[*.&#123;js,py&#125;]</span><br><span class="line">charset = utf-8</span><br><span class="line"></span><br><span class="line"><span class="comment"># 4 space indentation</span></span><br><span class="line">[*.py]</span><br><span class="line">indent_style = space</span><br><span class="line">indent_size = 4</span><br><span class="line"></span><br><span class="line"><span class="comment"># Tab indentation (no size specified)</span></span><br><span class="line">[Makefile]</span><br><span class="line">indent_style = tab</span><br><span class="line"></span><br><span class="line"><span class="comment"># Indentation override for all JS under lib directory</span></span><br><span class="line">[lib/**.js]</span><br><span class="line">indent_style = space</span><br><span class="line">indent_size = 2</span><br><span class="line"></span><br><span class="line"><span class="comment"># Matches the exact files either package.json or .travis.yml</span></span><br><span class="line">[&#123;package.json,.travis.yml&#125;]</span><br><span class="line">indent_style = space</span><br><span class="line">indent_size = 2</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>editorconfig</tag>
        <tag>engineering</tag>
      </tags>
  </entry>
  <entry>
    <title>oauth2</title>
    <url>/2021/07/10/oauth2/</url>
    <content><![CDATA[<p>OAuth 2.0<br><a href="http://www.ruanyifeng.com/blog/2014/05/oauth_2_0.html">讲解</a></p>
]]></content>
  </entry>
  <entry>
    <title>Vue项目搭建</title>
    <url>/2021/07/05/vue-template/</url>
    <content><![CDATA[<h2 id="基于Vue3-x的后台管理前端模板"><a href="#基于Vue3-x的后台管理前端模板" class="headerlink" title="基于Vue3.x的后台管理前端模板"></a>基于Vue3.x的后台管理前端模板</h2><blockquote>
<p>Vue vue-router vuex axios Stylus Echarts</p>
</blockquote>
<p>├── config                     // 配置相关<br>├── public                     // 静态资源，不会被webpack编译<br>│   ├── favicon.ico            // 网站图标<br>│   └── index.html             // html编译入口<br>├── src                        // 源代码<br>│   ├── api                    // axios，后台接口封装<br>│   ├── assets                 // 静态资源，会被webpack编译<br>│   ├── components             // 功能组件<br>│   ├── directive              // 全局指令<br>│   ├── filtres                // 全局 filter<br>│   ├── icons                  // 项目所有 svg icons<br>│   ├── lang                   // 国际化 language<br>│   ├── mock                   // 项目mock 模拟数据<br>│   ├── router                 // vue-router，路由配置<br>│   ├── store                  // vuex，store配置<br>│   ├── styles                 // 全局样式<br>│   ├── utils                  // 通用工具模块<br>│   ├── vendor                 // 公用vendor<br>│   ├── views                  // 视图组件<br>│   ├── App.vue                // vue入口<br>│   ├── main.js                // 项目总入口，加载组件 初始化等<br>│   └── permission.js          // 权限管理<br>├── static                     // 第三方不打包资源<br>│   └── Tinymce                // 富文本<br>├── babel.config.js            // babel 配置<br>├── eslintrc.js                // eslint 配置项<br>├── .gitignore                 // git 忽略项<br>├── README.md                  // 项目的说明文档<br>├── package.json               // node包管理<br>├── package-lock.json          // node包管理<br>└── vue.config.js              // 构建相关，webpack</p>
]]></content>
      <tags>
        <tag>Vue</tag>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Cache</title>
    <url>/2021/03/08/Spring-Cache/</url>
    <content><![CDATA[<h1 id="Spring-Cache-设计原理"><a href="#Spring-Cache-设计原理" class="headerlink" title="Spring Cache 设计原理"></a>Spring Cache 设计原理</h1><h2 id="Spring-Cache-API"><a href="#Spring-Cache-API" class="headerlink" title="Spring Cache API"></a>Spring Cache API</h2><p><code>Spring Cache API</code> 相关接口位于<code>spring-context</code>包，相对路径<code>org.springframework.cache</code>下。<br>核心接口 <code>org.springframework.cache.Cache</code> 定义通用缓存操作的接口，<code>org.springframework.cache.CacheManager</code> 缓存管理核心SPI，允许检索命名缓存区域</p>
<h3 id="Cache"><a href="#Cache" class="headerlink" title="Cache"></a>Cache</h3><p>该接口是对缓存操作的抽象，主要操作为 get put evict，可根据具体缓存实现对应方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Cache</span> </span>&#123;</span><br><span class="line">	<span class="comment">// 缓存名称</span></span><br><span class="line">	<span class="function">String <span class="title">getName</span><span class="params">()</span></span>;</span><br><span class="line">	<span class="comment">// 返回底层的缓存对象</span></span><br><span class="line">	<span class="function">Object <span class="title">getNativeCache</span><span class="params">()</span></span>;</span><br><span class="line">	<span class="comment">// 返回缓存键对应的值，使用 ValueWrapper 接口包装</span></span><br><span class="line">	<span class="meta">@Nullable</span></span><br><span class="line">	<span class="function">ValueWrapper <span class="title">get</span><span class="params">(Object key)</span></span>;</span><br><span class="line">	<span class="comment">// 返回值自动转换为指定类型</span></span><br><span class="line">	<span class="meta">@Nullable</span></span><br><span class="line">	&lt;T&gt; <span class="function">T <span class="title">get</span><span class="params">(Object key, <span class="meta">@Nullable</span> Class&lt;T&gt; type)</span></span>;</span><br><span class="line">	<span class="comment">// 用于同步查询缓存，同步保证由具体实现负责</span></span><br><span class="line">	<span class="meta">@Nullable</span></span><br><span class="line">	&lt;T&gt; <span class="function">T <span class="title">get</span><span class="params">(Object key, Callable&lt;T&gt; valueLoader)</span></span>;</span><br><span class="line">	<span class="comment">// 放置缓存</span></span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">put</span><span class="params">(Object key, <span class="meta">@Nullable</span> Object value)</span></span>;</span><br><span class="line">	<span class="comment">// 指定 key 不存在，放置新的 value 否则返回原有 value</span></span><br><span class="line">	<span class="meta">@Nullable</span></span><br><span class="line">	<span class="function"><span class="keyword">default</span> ValueWrapper <span class="title">putIfAbsent</span><span class="params">(Object key, <span class="meta">@Nullable</span> Object value)</span> </span>&#123;</span><br><span class="line">		ValueWrapper existingValue = get(key);</span><br><span class="line">		<span class="keyword">if</span> (existingValue == <span class="keyword">null</span>) &#123;</span><br><span class="line">			put(key, value);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> existingValue;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// 删除指定缓存</span></span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">evict</span><span class="params">(Object key)</span></span>;</span><br><span class="line">	<span class="comment">// 删除指定缓存</span></span><br><span class="line">	<span class="function"><span class="keyword">default</span> <span class="keyword">boolean</span> <span class="title">evictIfPresent</span><span class="params">(Object key)</span> </span>&#123;</span><br><span class="line">		evict(key);</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// 清空缓存</span></span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">clear</span><span class="params">()</span></span>;</span><br><span class="line">	<span class="comment">// 清空缓存</span></span><br><span class="line">	<span class="function"><span class="keyword">default</span> <span class="keyword">boolean</span> <span class="title">invalidate</span><span class="params">()</span> </span>&#123;</span><br><span class="line">		clear();</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>以 Redis 缓存实现为例</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RedisCache</span> <span class="keyword">extends</span> <span class="title">AbstractValueAdaptingCache</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">byte</span>[] BINARY_NULL_VALUE = RedisSerializer.java().serialize(NullValue.INSTANCE);</span><br><span class="line">	<span class="comment">// 缓存名称</span></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">final</span> String name;</span><br><span class="line">	<span class="comment">// redis 操作的实现类</span></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">final</span> RedisCacheWriter cacheWriter;</span><br><span class="line">	<span class="comment">// reids 配置</span></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">final</span> RedisCacheConfiguration cacheConfig;</span><br><span class="line">	<span class="comment">// DefaultFormattingConversionService spring 提供的类型转换能力</span></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">final</span> ConversionService conversionService;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 实际查找实现</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="function"><span class="keyword">protected</span> Object <span class="title">lookup</span><span class="params">(Object key)</span> </span>&#123;</span><br><span class="line">		<span class="comment">// 从 redis 中检索</span></span><br><span class="line">		<span class="keyword">byte</span>[] value = cacheWriter.get(name, createAndConvertCacheKey(key));</span><br><span class="line">		<span class="keyword">if</span> (value == <span class="keyword">null</span>) &#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">// 反序列化</span></span><br><span class="line">		<span class="keyword">return</span> deserializeCacheValue(value);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 返回缓存名称</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">		<span class="keyword">return</span> name;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 返回底层缓存实例</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> RedisCacheWriter <span class="title">getNativeCache</span><span class="params">()</span> </span>&#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">this</span>.cacheWriter;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// get 实现，使用 synchronized 实现同步</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="meta">@SuppressWarnings(&quot;unchecked&quot;)</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">synchronized</span> &lt;T&gt; <span class="function">T <span class="title">get</span><span class="params">(Object key, Callable&lt;T&gt; valueLoader)</span> </span>&#123;</span><br><span class="line">		<span class="comment">// 缓存中查询</span></span><br><span class="line">		ValueWrapper result = get(key);</span><br><span class="line">		<span class="comment">// 缓存命中直接返回</span></span><br><span class="line">		<span class="keyword">if</span> (result != <span class="keyword">null</span>) &#123;</span><br><span class="line">			<span class="keyword">return</span> (T) result.get();</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">// 执行取值逻辑</span></span><br><span class="line">		T value = valueFromLoader(key, valueLoader);</span><br><span class="line">		<span class="comment">// 放置缓存</span></span><br><span class="line">		put(key, value);</span><br><span class="line">		<span class="keyword">return</span> value;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// put 实现</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">put</span><span class="params">(Object key, <span class="meta">@Nullable</span> Object value)</span> </span>&#123;</span><br><span class="line">		<span class="comment">// 空值处理</span></span><br><span class="line">		Object cacheValue = preProcessCacheValue(value);</span><br><span class="line">		<span class="keyword">if</span> (!isAllowNullValues() &amp;&amp; cacheValue == <span class="keyword">null</span>) &#123;</span><br><span class="line">			<span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException(String.format(<span class="string">&quot;Cache &#x27;%s&#x27; does not allow &#x27;null&#x27; values. Avoid storing null via &#x27;@Cacheable(unless=\&quot;#result == null\&quot;)&#x27; or configure RedisCache to allow &#x27;null&#x27; via RedisCacheConfiguration.&quot;</span>, name));</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">// redis 操作</span></span><br><span class="line">		cacheWriter.put(name, createAndConvertCacheKey(key), serializeCacheValue(cacheValue), cacheConfig.getTtl());</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// putIfAbsent 实现</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> ValueWrapper <span class="title">putIfAbsent</span><span class="params">(Object key, <span class="meta">@Nullable</span> Object value)</span> </span>&#123;</span><br><span class="line">		<span class="comment">// 空值处理</span></span><br><span class="line">		Object cacheValue = preProcessCacheValue(value);</span><br><span class="line">		<span class="keyword">if</span> (!isAllowNullValues() &amp;&amp; cacheValue == <span class="keyword">null</span>) &#123;</span><br><span class="line">			<span class="keyword">return</span> get(key);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">// redis 操作</span></span><br><span class="line">		<span class="keyword">byte</span>[] result = cacheWriter.putIfAbsent(name, createAndConvertCacheKey(key), serializeCacheValue(cacheValue), cacheConfig.getTtl());</span><br><span class="line">		<span class="keyword">if</span> (result == <span class="keyword">null</span>) &#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">new</span> SimpleValueWrapper(fromStoreValue(deserializeCacheValue(result)));</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// evict 实现</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">evict</span><span class="params">(Object key)</span> </span>&#123;</span><br><span class="line">		<span class="comment">// redis 操作</span></span><br><span class="line">		cacheWriter.remove(name, createAndConvertCacheKey(key));</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// clear 实现</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">clear</span><span class="params">()</span> </span>&#123;</span><br><span class="line">		<span class="comment">// 类型转换</span></span><br><span class="line">		<span class="keyword">byte</span>[] pattern = conversionService.convert(createCacheKey(<span class="string">&quot;*&quot;</span>), <span class="keyword">byte</span>[].class);</span><br><span class="line">		<span class="comment">// redis 操作</span></span><br><span class="line">		cacheWriter.clean(name, pattern);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="CacheManager"><a href="#CacheManager" class="headerlink" title="CacheManager"></a>CacheManager</h3><p>缓存管理器抽象，根据具体缓存实现</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">CacheManager</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 根据名称获得缓存实例</span></span><br><span class="line">	<span class="function">Cache <span class="title">getCache</span><span class="params">(String name)</span></span>;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 返回该缓存管理器下的所有缓存名称</span></span><br><span class="line">	<span class="function">Collection&lt;String&gt; <span class="title">getCacheNames</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>以 Redis 缓存管理器为例</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">AbstractCacheManager</span> <span class="keyword">implements</span> <span class="title">CacheManager</span>, <span class="title">InitializingBean</span> </span>&#123;	</span><br><span class="line">	<span class="comment">// 本地缓存 name -&gt; Cache</span></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">final</span> ConcurrentMap&lt;String, Cache&gt; cacheMap = <span class="keyword">new</span> ConcurrentHashMap&lt;&gt;(<span class="number">16</span>);</span><br><span class="line">	<span class="comment">// 保存的缓存名称</span></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">volatile</span> Set&lt;String&gt; cacheNames = Collections.emptySet();</span><br><span class="line"></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="meta">@Nullable</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> Cache <span class="title">getCache</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">		<span class="comment">// 本地缓存命中直接返回</span></span><br><span class="line">		Cache cache = <span class="keyword">this</span>.cacheMap.get(name);</span><br><span class="line">		<span class="keyword">if</span> (cache != <span class="keyword">null</span>) &#123;</span><br><span class="line">			<span class="keyword">return</span> cache;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 子类实现，创建缓存实例</span></span><br><span class="line">		Cache missingCache = getMissingCache(name);</span><br><span class="line">		<span class="keyword">if</span> (missingCache != <span class="keyword">null</span>) &#123;</span><br><span class="line">			<span class="comment">// 创建操作同步</span></span><br><span class="line">			<span class="keyword">synchronized</span> (<span class="keyword">this</span>.cacheMap) &#123;</span><br><span class="line">				cache = <span class="keyword">this</span>.cacheMap.get(name);</span><br><span class="line">				<span class="keyword">if</span> (cache == <span class="keyword">null</span>) &#123;</span><br><span class="line">					cache = decorateCache(missingCache);</span><br><span class="line">					<span class="keyword">this</span>.cacheMap.put(name, cache);</span><br><span class="line">					updateCacheNames(name);</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> cache;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RedisCacheManager</span> <span class="keyword">extends</span> <span class="title">AbstractTransactionSupportingCacheManager</span> </span>&#123;</span><br><span class="line">	<span class="comment">// 创建缓存实例</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="function"><span class="keyword">protected</span> RedisCache <span class="title">getMissingCache</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">return</span> allowInFlightCacheCreation ? createRedisCache(name, defaultCacheConfig) : <span class="keyword">null</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// 实例化 RedisCache</span></span><br><span class="line">	<span class="function"><span class="keyword">protected</span> RedisCache <span class="title">createRedisCache</span><span class="params">(String name, <span class="meta">@Nullable</span> RedisCacheConfiguration cacheConfig)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">new</span> RedisCache(name, cacheWriter, cacheConfig != <span class="keyword">null</span> ? cacheConfig : defaultCacheConfig);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="缓存注解原理"><a href="#缓存注解原理" class="headerlink" title="缓存注解原理"></a>缓存注解原理</h2><h3 id="EnableCaching"><a href="#EnableCaching" class="headerlink" title="@EnableCaching"></a>@EnableCaching</h3><p>使用<code>@EnableCaching</code> 可以开启注解支持，该注解导入了一个<code>CachingConfigurationSelector</code>，根据注解的mode属性，导入相应的配置类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Target(ElementType.TYPE)</span></span><br><span class="line"><span class="meta">@Retention(RetentionPolicy.RUNTIME)</span></span><br><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="meta">@Import(CachingConfigurationSelector.class)</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> EnableCaching &#123;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * 指定代理类型，mode()设置为AdviceMode.PROXY时生效，影响所有Spring管理的Bean，默认使用jdk动态代理，true代表使用CGLIB代理</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="function"><span class="keyword">boolean</span> <span class="title">proxyTargetClass</span><span class="params">()</span> <span class="keyword">default</span> <span class="keyword">false</span></span>;</span><br><span class="line">      </span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * 指定缓存如何生效，默认使用动态代理方式，可升级为静态织入AdviceMode.ASPECTJ</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="function">AdviceMode <span class="title">mode</span><span class="params">()</span> <span class="keyword">default</span> AdviceMode.PROXY</span>;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * 指定缓存相关通知的执行顺序，当连接点存在多个通知时</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="function"><span class="keyword">int</span> <span class="title">order</span><span class="params">()</span> <span class="keyword">default</span> Ordered.LOWEST_PRECEDENCE</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="CachingConfigurationSelector"><a href="#CachingConfigurationSelector" class="headerlink" title="CachingConfigurationSelector"></a>CachingConfigurationSelector</h3><p><code>CachingConfigurationSelector</code>实现了<code>ImportSelector</code>接口，可以获得注解元数据，根据元数据导入对应配置类。<br>默认导入<code>AutoProxyRegistrar</code>和<code>ProxyCachingConfiguration</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CachingConfigurationSelector</span> <span class="keyword">extends</span> <span class="title">AdviceModeImportSelector</span>&lt;<span class="title">EnableCaching</span>&gt; </span>&#123;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> String PROXY_JCACHE_CONFIGURATION_CLASS =	<span class="string">&quot;org.springframework.cache.jcache.config.ProxyJCacheConfiguration&quot;</span>;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> String CACHE_ASPECT_CONFIGURATION_CLASS_NAME = <span class="string">&quot;org.springframework.cache.aspectj.AspectJCachingConfiguration&quot;</span>;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> String JCACHE_ASPECT_CONFIGURATION_CLASS_NAME =	<span class="string">&quot;org.springframework.cache.aspectj.AspectJJCacheConfiguration&quot;</span>;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// jsr-107规范</span></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">boolean</span> jsr107Present;</span><br><span class="line">   <span class="comment">// jcache实现</span></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">boolean</span> jcacheImplPresent;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">static</span> &#123;</span><br><span class="line">		ClassLoader classLoader = CachingConfigurationSelector.class.getClassLoader();</span><br><span class="line">		jsr107Present = ClassUtils.isPresent(<span class="string">&quot;javax.cache.Cache&quot;</span>, classLoader);</span><br><span class="line">		jcacheImplPresent = ClassUtils.isPresent(PROXY_JCACHE_CONFIGURATION_CLASS, classLoader);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * 根据mode决定导入的类</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">public</span> String[] selectImports(AdviceMode adviceMode) &#123;</span><br><span class="line">		<span class="keyword">switch</span> (adviceMode) &#123;</span><br><span class="line">			<span class="keyword">case</span> PROXY:</span><br><span class="line">				<span class="keyword">return</span> getProxyImports();</span><br><span class="line">			<span class="keyword">case</span> ASPECTJ:</span><br><span class="line">				<span class="keyword">return</span> getAspectJImports();</span><br><span class="line">			<span class="keyword">default</span>:</span><br><span class="line">				<span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * 导入 `AutoProxyRegistrar` 和 `ProxyCachingConfiguration`</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="keyword">private</span> String[] getProxyImports() &#123;</span><br><span class="line">		List&lt;String&gt; result = <span class="keyword">new</span> ArrayList&lt;&gt;(<span class="number">3</span>);</span><br><span class="line">		result.add(AutoProxyRegistrar.class.getName());</span><br><span class="line">		result.add(ProxyCachingConfiguration.class.getName());</span><br><span class="line">		<span class="comment">// 根据对应类是否存在判断是否加载jsr规范的自动配置类</span></span><br><span class="line">		<span class="keyword">if</span> (jsr107Present &amp;&amp; jcacheImplPresent) &#123;</span><br><span class="line">			result.add(PROXY_JCACHE_CONFIGURATION_CLASS);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> StringUtils.toStringArray(result);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * AdviceMode#ASPECTJ 配置类</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="keyword">private</span> String[] getAspectJImports() &#123;</span><br><span class="line">		List&lt;String&gt; result = <span class="keyword">new</span> ArrayList&lt;&gt;(<span class="number">2</span>);</span><br><span class="line">		result.add(CACHE_ASPECT_CONFIGURATION_CLASS_NAME);</span><br><span class="line">		<span class="keyword">if</span> (jsr107Present &amp;&amp; jcacheImplPresent) &#123;</span><br><span class="line">			result.add(JCACHE_ASPECT_CONFIGURATION_CLASS_NAME);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> StringUtils.toStringArray(result);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="AutoProxyRegistrar"><a href="#AutoProxyRegistrar" class="headerlink" title="AutoProxyRegistrar"></a><code>AutoProxyRegistrar</code></h3><p>自动代理创建器注册器，用于在容器中注册一个<code>AbstractAutoProxyCreator</code>，通常是<code>AnnotationAwareAspectJAutoProxyCreator</code>，它是一个<code>SmartInstantiationAwareBeanPostProcessor</code>，在bean注入或者初始化化之后返回代理后的实例。构造代理对象时，会从容器中查找符合条件的通知器<code>Advisor</code>应用到代理对象，从而实现缓存相关功能的实现。</p>
<h3 id="ProxyCachingConfiguration-extends-AbstractCachingConfiguration"><a href="#ProxyCachingConfiguration-extends-AbstractCachingConfiguration" class="headerlink" title="ProxyCachingConfiguration extends AbstractCachingConfiguration"></a><code>ProxyCachingConfiguration</code> extends <code>AbstractCachingConfiguration</code></h3><p>缓存代理功能组件配置，主要实现以下功能</p>
<ol>
<li>注入<code>CachingConfigurer</code></li>
<li>声明<code>AnnotationCacheOperationSource</code></li>
<li>声明<code>CacheInterceptor</code></li>
<li>声明<code>BeanFactoryCacheOperationSourceAdvisor</code></li>
</ol>
<h4 id="CachingConfigurer"><a href="#CachingConfigurer" class="headerlink" title="CachingConfigurer"></a>CachingConfigurer</h4><p>缓存配置自定义，可实现该接口自定义缓存相关的<code>CacheManager</code>，<code>CacheResolver</code>，<code>KeyGenerator</code>，<code>CacheErrorHandler</code>，也可以继承<code>CachingConfigurerSupport</code>，选择实现部分功能</p>
<h4 id="AnnotationCacheOperationSource"><a href="#AnnotationCacheOperationSource" class="headerlink" title="AnnotationCacheOperationSource"></a>AnnotationCacheOperationSource</h4><p>实现接口<code>CacheOperationSource</code>，给<code>CacheInterceptor</code>拦截器提供缓存操作所需的属性，该实现类从<code>Spring</code>缓存相关注解中读取元数据，默认内置<code>SpringCacheAnnotationParser</code>从方法或类上解析注解的元数据并缓存</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AnnotationCacheOperationSource</span> <span class="keyword">extends</span> <span class="title">AbstractFallbackCacheOperationSource</span> <span class="keyword">implements</span> <span class="title">Serializable</span> </span>&#123;</span><br><span class="line">   <span class="comment">// 标识只支持public方法</span></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">boolean</span> publicMethodsOnly;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">final</span> Set&lt;CacheAnnotationParser&gt; annotationParsers;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">    * 默认构造方法，只支持public方法</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="title">AnnotationCacheOperationSource</span><span class="params">()</span> </span>&#123;</span><br><span class="line">		<span class="keyword">this</span>(<span class="keyword">true</span>);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * 默认使用 SpringCacheAnnotationParser 注解解析器</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="title">AnnotationCacheOperationSource</span><span class="params">(<span class="keyword">boolean</span> publicMethodsOnly)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">this</span>.publicMethodsOnly = publicMethodsOnly;</span><br><span class="line">		<span class="keyword">this</span>.annotationParsers = Collections.singleton(<span class="keyword">new</span> SpringCacheAnnotationParser());</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="title">AnnotationCacheOperationSource</span><span class="params">(CacheAnnotationParser annotationParser)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">this</span>.publicMethodsOnly = <span class="keyword">true</span>;</span><br><span class="line">		Assert.notNull(annotationParser, <span class="string">&quot;CacheAnnotationParser must not be null&quot;</span>);</span><br><span class="line">		<span class="keyword">this</span>.annotationParsers = Collections.singleton(annotationParser);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="title">AnnotationCacheOperationSource</span><span class="params">(CacheAnnotationParser... annotationParsers)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">this</span>.publicMethodsOnly = <span class="keyword">true</span>;</span><br><span class="line">		Assert.notEmpty(annotationParsers, <span class="string">&quot;At least one CacheAnnotationParser needs to be specified&quot;</span>);</span><br><span class="line">		<span class="keyword">this</span>.annotationParsers = <span class="keyword">new</span> LinkedHashSet&lt;&gt;(Arrays.asList(annotationParsers));</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="title">AnnotationCacheOperationSource</span><span class="params">(Set&lt;CacheAnnotationParser&gt; annotationParsers)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">this</span>.publicMethodsOnly = <span class="keyword">true</span>;</span><br><span class="line">		Assert.notEmpty(annotationParsers, <span class="string">&quot;At least one CacheAnnotationParser needs to be specified&quot;</span>);</span><br><span class="line">		<span class="keyword">this</span>.annotationParsers = annotationParsers;</span><br><span class="line">	&#125;</span><br><span class="line">   <span class="comment">// 判断注解解析器是否支持指定类</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">isCandidateClass</span><span class="params">(Class&lt;?&gt; targetClass)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">for</span> (CacheAnnotationParser parser : <span class="keyword">this</span>.annotationParsers) &#123;</span><br><span class="line">			<span class="keyword">if</span> (parser.isCandidateClass(targetClass)) &#123;</span><br><span class="line">				<span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">	&#125;</span><br><span class="line">   <span class="comment">// 解析类，委托解析器执行</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="meta">@Nullable</span></span><br><span class="line">	<span class="function"><span class="keyword">protected</span> Collection&lt;CacheOperation&gt; <span class="title">findCacheOperations</span><span class="params">(Class&lt;?&gt; clazz)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">return</span> determineCacheOperations(parser -&gt; parser.parseCacheAnnotations(clazz));</span><br><span class="line">	&#125;</span><br><span class="line">   <span class="comment">// 解析方法，委托解析器执行</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="meta">@Nullable</span></span><br><span class="line">	<span class="function"><span class="keyword">protected</span> Collection&lt;CacheOperation&gt; <span class="title">findCacheOperations</span><span class="params">(Method method)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">return</span> determineCacheOperations(parser -&gt; parser.parseCacheAnnotations(method));</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * 通过解析器分析注解信息，封装为 CacheOperation 元数据类</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="meta">@Nullable</span></span><br><span class="line">	<span class="function"><span class="keyword">protected</span> Collection&lt;CacheOperation&gt; <span class="title">determineCacheOperations</span><span class="params">(CacheOperationProvider provider)</span> </span>&#123;</span><br><span class="line">		Collection&lt;CacheOperation&gt; ops = <span class="keyword">null</span>;</span><br><span class="line">		<span class="keyword">for</span> (CacheAnnotationParser parser : <span class="keyword">this</span>.annotationParsers) &#123;</span><br><span class="line">			Collection&lt;CacheOperation&gt; annOps = provider.getCacheOperations(parser);</span><br><span class="line">			<span class="keyword">if</span> (annOps != <span class="keyword">null</span>) &#123;</span><br><span class="line">				<span class="keyword">if</span> (ops == <span class="keyword">null</span>) &#123;</span><br><span class="line">					ops = annOps;</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">else</span> &#123;</span><br><span class="line">					Collection&lt;CacheOperation&gt; combined = <span class="keyword">new</span> ArrayList&lt;&gt;(ops.size() + annOps.size());</span><br><span class="line">					combined.addAll(ops);</span><br><span class="line">					combined.addAll(annOps);</span><br><span class="line">					ops = combined;</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> ops;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>实际的解析动作由<code>SpringCacheAnnotationParser</code>完成，解析对应注解上的属性，封装为<code>CacheableOperation</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SpringCacheAnnotationParser</span> <span class="keyword">implements</span> <span class="title">CacheAnnotationParser</span>, <span class="title">Serializable</span> </span>&#123;</span><br><span class="line">	<span class="comment">// 支持的缓存操作注解</span></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> Set&lt;Class&lt;? extends Annotation&gt;&gt; CACHE_OPERATION_ANNOTATIONS = <span class="keyword">new</span> LinkedHashSet&lt;&gt;(<span class="number">8</span>);</span><br><span class="line">	<span class="comment">// 初始化支持的操作注解</span></span><br><span class="line">	<span class="keyword">static</span> &#123;</span><br><span class="line">		CACHE_OPERATION_ANNOTATIONS.add(Cacheable.class);</span><br><span class="line">		CACHE_OPERATION_ANNOTATIONS.add(CacheEvict.class);</span><br><span class="line">		CACHE_OPERATION_ANNOTATIONS.add(CachePut.class);</span><br><span class="line">		CACHE_OPERATION_ANNOTATIONS.add(Caching.class);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// 分析目标类上是否有指定的注解</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">isCandidateClass</span><span class="params">(Class&lt;?&gt; targetClass)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">return</span> AnnotationUtils.isCandidateClass(targetClass, CACHE_OPERATION_ANNOTATIONS);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// 分析类</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="meta">@Nullable</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> Collection&lt;CacheOperation&gt; <span class="title">parseCacheAnnotations</span><span class="params">(Class&lt;?&gt; type)</span> </span>&#123;</span><br><span class="line">		DefaultCacheConfig defaultConfig = <span class="keyword">new</span> DefaultCacheConfig(type);</span><br><span class="line">		<span class="keyword">return</span> parseCacheAnnotations(defaultConfig, type);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// 分析方法，如果类上使用了 `@CacheConfig` 可设置类级别的默认设置</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="meta">@Nullable</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> Collection&lt;CacheOperation&gt; <span class="title">parseCacheAnnotations</span><span class="params">(Method method)</span> </span>&#123;</span><br><span class="line">		DefaultCacheConfig defaultConfig = <span class="keyword">new</span> DefaultCacheConfig(method.getDeclaringClass());</span><br><span class="line">		<span class="keyword">return</span> parseCacheAnnotations(defaultConfig, method);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// 解析缓存注解</span></span><br><span class="line">	<span class="meta">@Nullable</span></span><br><span class="line">	<span class="function"><span class="keyword">private</span> Collection&lt;CacheOperation&gt; <span class="title">parseCacheAnnotations</span><span class="params">(DefaultCacheConfig cachingConfig, AnnotatedElement ae)</span> </span>&#123;</span><br><span class="line">		Collection&lt;CacheOperation&gt; ops = parseCacheAnnotations(cachingConfig, ae, <span class="keyword">false</span>);</span><br><span class="line">		<span class="keyword">if</span> (ops != <span class="keyword">null</span> &amp;&amp; ops.size() &gt; <span class="number">1</span>) &#123;</span><br><span class="line">			<span class="comment">// 解析出多个注解，本类的覆盖接口上的</span></span><br><span class="line">			Collection&lt;CacheOperation&gt; localOps = parseCacheAnnotations(cachingConfig, ae, <span class="keyword">true</span>);</span><br><span class="line">			<span class="keyword">if</span> (localOps != <span class="keyword">null</span>) &#123;</span><br><span class="line">				<span class="keyword">return</span> localOps;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> ops;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// 解析缓存注解， localOnly 指示是否本类优先</span></span><br><span class="line">	<span class="meta">@Nullable</span></span><br><span class="line">	<span class="function"><span class="keyword">private</span> Collection&lt;CacheOperation&gt; <span class="title">parseCacheAnnotations</span><span class="params">(DefaultCacheConfig cachingConfig, AnnotatedElement ae, <span class="keyword">boolean</span> localOnly)</span> </span>&#123;</span><br><span class="line">		Collection&lt;? extends Annotation&gt; anns = (localOnly ?</span><br><span class="line">				<span class="comment">// 不向上递归查找</span></span><br><span class="line">				AnnotatedElementUtils.getAllMergedAnnotations(ae, CACHE_OPERATION_ANNOTATIONS) :</span><br><span class="line">				<span class="comment">// 递归查找</span></span><br><span class="line">				AnnotatedElementUtils.findAllMergedAnnotations(ae, CACHE_OPERATION_ANNOTATIONS));</span><br><span class="line">		<span class="keyword">if</span> (anns.isEmpty()) &#123;</span><br><span class="line">			<span class="comment">// 没有找到注解直接返回null</span></span><br><span class="line">			<span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">final</span> Collection&lt;CacheOperation&gt; ops = <span class="keyword">new</span> ArrayList&lt;&gt;(<span class="number">1</span>);</span><br><span class="line">		<span class="comment">// 解析Cacheable</span></span><br><span class="line">		anns.stream().filter(ann -&gt; ann <span class="keyword">instanceof</span> Cacheable).forEach(ann -&gt; ops.add(parseCacheableAnnotation(ae, cachingConfig, (Cacheable) ann)));</span><br><span class="line">		<span class="comment">// 解析CacheEvict</span></span><br><span class="line">		anns.stream().filter(ann -&gt; ann <span class="keyword">instanceof</span> CacheEvict).forEach(ann -&gt; ops.add(parseEvictAnnotation(ae, cachingConfig, (CacheEvict) ann)));</span><br><span class="line">		<span class="comment">// 解析CachePut</span></span><br><span class="line">		anns.stream().filter(ann -&gt; ann <span class="keyword">instanceof</span> CachePut).forEach(ann -&gt; ops.add(parsePutAnnotation(ae, cachingConfig, (CachePut) ann)));</span><br><span class="line">		<span class="comment">// 解析Caching</span></span><br><span class="line">		anns.stream().filter(ann -&gt; ann <span class="keyword">instanceof</span> Caching).forEach(ann -&gt; parseCachingAnnotation(ae, cachingConfig, (Caching) ann, ops));</span><br><span class="line">		<span class="keyword">return</span> ops;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// 注解属性大部分一样，使用建造者模式</span></span><br><span class="line">	<span class="comment">// cacheNames 缓存名</span></span><br><span class="line">	<span class="comment">// condition 条件 SpEL表达式</span></span><br><span class="line">	<span class="comment">// unless 否决 SpEL表达式</span></span><br><span class="line">	<span class="comment">// key 缓存键 SpEL表达式 不可同时指定 key 和 keyGenerator</span></span><br><span class="line">	<span class="comment">// keyGenerator 缓存键生成器的bean名称</span></span><br><span class="line">	<span class="comment">// cacheManager 缓存管理器 不可同时指定 cacheManager 和 cacheResolver</span></span><br><span class="line">	<span class="comment">// cacheResolver 缓存解析器</span></span><br><span class="line">	<span class="function"><span class="keyword">private</span> CacheableOperation <span class="title">parseCacheableAnnotation</span><span class="params">(AnnotatedElement ae, DefaultCacheConfig defaultConfig, Cacheable cacheable)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">		CacheableOperation.Builder builder = <span class="keyword">new</span> CacheableOperation.Builder();</span><br><span class="line"></span><br><span class="line">		builder.setName(ae.toString());</span><br><span class="line">		builder.setCacheNames(cacheable.cacheNames());</span><br><span class="line">		builder.setCondition(cacheable.condition());</span><br><span class="line">		builder.setUnless(cacheable.unless());</span><br><span class="line">		builder.setKey(cacheable.key());</span><br><span class="line">		builder.setKeyGenerator(cacheable.keyGenerator());</span><br><span class="line">		builder.setCacheManager(cacheable.cacheManager());</span><br><span class="line">		builder.setCacheResolver(cacheable.cacheResolver());</span><br><span class="line">		builder.setSync(cacheable.sync());</span><br><span class="line"></span><br><span class="line">		defaultConfig.applyDefault(builder);</span><br><span class="line">		CacheableOperation op = builder.build();</span><br><span class="line">		validateCacheOperation(ae, op);</span><br><span class="line"></span><br><span class="line">		<span class="keyword">return</span> op;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="keyword">private</span> CacheEvictOperation <span class="title">parseEvictAnnotation</span><span class="params">(AnnotatedElement ae, DefaultCacheConfig defaultConfig, CacheEvict cacheEvict)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">		CacheEvictOperation.Builder builder = <span class="keyword">new</span> CacheEvictOperation.Builder();</span><br><span class="line"></span><br><span class="line">		builder.setName(ae.toString());</span><br><span class="line">		builder.setCacheNames(cacheEvict.cacheNames());</span><br><span class="line">		builder.setCondition(cacheEvict.condition());</span><br><span class="line">		builder.setKey(cacheEvict.key());</span><br><span class="line">		builder.setKeyGenerator(cacheEvict.keyGenerator());</span><br><span class="line">		builder.setCacheManager(cacheEvict.cacheManager());</span><br><span class="line">		builder.setCacheResolver(cacheEvict.cacheResolver());</span><br><span class="line">		builder.setCacheWide(cacheEvict.allEntries());</span><br><span class="line">		builder.setBeforeInvocation(cacheEvict.beforeInvocation());</span><br><span class="line"></span><br><span class="line">		defaultConfig.applyDefault(builder);</span><br><span class="line">		CacheEvictOperation op = builder.build();</span><br><span class="line">		validateCacheOperation(ae, op);</span><br><span class="line"></span><br><span class="line">		<span class="keyword">return</span> op;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="keyword">private</span> CacheOperation <span class="title">parsePutAnnotation</span><span class="params">(AnnotatedElement ae, DefaultCacheConfig defaultConfig, CachePut cachePut)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">		CachePutOperation.Builder builder = <span class="keyword">new</span> CachePutOperation.Builder();</span><br><span class="line"></span><br><span class="line">		builder.setName(ae.toString());</span><br><span class="line">		builder.setCacheNames(cachePut.cacheNames());</span><br><span class="line">		builder.setCondition(cachePut.condition());</span><br><span class="line">		builder.setUnless(cachePut.unless());</span><br><span class="line">		builder.setKey(cachePut.key());</span><br><span class="line">		builder.setKeyGenerator(cachePut.keyGenerator());</span><br><span class="line">		builder.setCacheManager(cachePut.cacheManager());</span><br><span class="line">		builder.setCacheResolver(cachePut.cacheResolver());</span><br><span class="line"></span><br><span class="line">		defaultConfig.applyDefault(builder);</span><br><span class="line">		CachePutOperation op = builder.build();</span><br><span class="line">		validateCacheOperation(ae, op);</span><br><span class="line"></span><br><span class="line">		<span class="keyword">return</span> op;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// 复合注解 分别解析</span></span><br><span class="line">	<span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">parseCachingAnnotation</span><span class="params">(AnnotatedElement ae, DefaultCacheConfig defaultConfig, Caching caching, Collection&lt;CacheOperation&gt; ops)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">		Cacheable[] cacheables = caching.cacheable();</span><br><span class="line">		<span class="keyword">for</span> (Cacheable cacheable : cacheables) &#123;</span><br><span class="line">			ops.add(parseCacheableAnnotation(ae, defaultConfig, cacheable));</span><br><span class="line">		&#125;</span><br><span class="line">		CacheEvict[] cacheEvicts = caching.evict();</span><br><span class="line">		<span class="keyword">for</span> (CacheEvict cacheEvict : cacheEvicts) &#123;</span><br><span class="line">			ops.add(parseEvictAnnotation(ae, defaultConfig, cacheEvict));</span><br><span class="line">		&#125;</span><br><span class="line">		CachePut[] cachePuts = caching.put();</span><br><span class="line">		<span class="keyword">for</span> (CachePut cachePut : cachePuts) &#123;</span><br><span class="line">			ops.add(parsePutAnnotation(ae, defaultConfig, cachePut));</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="CacheInterceptor"><a href="#CacheInterceptor" class="headerlink" title="CacheInterceptor"></a>CacheInterceptor</h4><p>实现AOP联盟接口<code>MethodInterceptor</code>，用于使用通用<code>Spring</code>缓存基础结构的声明式缓存管理。继承自<code>CacheAspectSupport</code>，包含了与<code>Spring</code>底层缓存API的集成。<br>主要操作流程在 <code>CacheAspectSupport.execute</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CacheInterceptor</span> <span class="keyword">extends</span> <span class="title">CacheAspectSupport</span> <span class="keyword">implements</span> <span class="title">MethodInterceptor</span>, <span class="title">Serializable</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="meta">@Nullable</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> Object <span class="title">invoke</span><span class="params">(<span class="keyword">final</span> MethodInvocation invocation)</span> <span class="keyword">throws</span> Throwable </span>&#123;</span><br><span class="line">		Method method = invocation.getMethod();</span><br><span class="line"></span><br><span class="line">		CacheOperationInvoker aopAllianceInvoker = () -&gt; &#123;</span><br><span class="line">			<span class="keyword">try</span> &#123;</span><br><span class="line">				<span class="comment">// 调用 MethodInvocation 处理</span></span><br><span class="line">				<span class="keyword">return</span> invocation.proceed();</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">				<span class="keyword">throw</span> <span class="keyword">new</span> CacheOperationInvoker.ThrowableWrapper(ex);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">try</span> &#123;</span><br><span class="line">			<span class="comment">// 调用CacheAspectSupport.execute</span></span><br><span class="line">			<span class="keyword">return</span> execute(aopAllianceInvoker, invocation.getThis(), method, invocation.getArguments());</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">catch</span> (CacheOperationInvoker.ThrowableWrapper th) &#123;</span><br><span class="line">			<span class="keyword">throw</span> th.getOriginal();</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>CacheAspectSupport</code>，该类包含与<code>Spring</code>底层缓存API的集成，使用策略设计模式。<code>CacheOperationSource</code>用于确定缓存操作，<code>KeyGenerator</code>将构建缓存键，<code>CacheResolver</code>将解析要使用的实际缓存。<code>BeanFactoryAware</code>接口自动注入<code>BeanFactory</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">CacheAspectSupport</span> <span class="keyword">extends</span> <span class="title">AbstractCacheInvoker</span></span></span><br><span class="line"><span class="class">		<span class="keyword">implements</span> <span class="title">BeanFactoryAware</span>, <span class="title">InitializingBean</span>, <span class="title">SmartInitializingSingleton</span> </span>&#123;</span><br><span class="line">	<span class="comment">// CacheOperation 元数据的缓存</span></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">final</span> Map&lt;CacheOperationCacheKey, CacheOperationMetadata&gt; metadataCache = <span class="keyword">new</span> ConcurrentHashMap&lt;&gt;(<span class="number">1024</span>);</span><br><span class="line">	<span class="comment">// SpEL表达式解析的工具类</span></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">final</span> CacheOperationExpressionEvaluator evaluator = <span class="keyword">new</span> CacheOperationExpressionEvaluator();</span><br><span class="line">	<span class="comment">// 此处可配置为 CompositeCacheOperationSource ，用于迭代多个 CacheOperationSource</span></span><br><span class="line">	<span class="comment">// 自动配置类自动注入 AnnotationCacheOperationSource</span></span><br><span class="line">	<span class="keyword">private</span> CacheOperationSource cacheOperationSource;</span><br><span class="line">	<span class="comment">// 缓存键生成器，可通过 CachingConfigurer 自定义</span></span><br><span class="line">	<span class="keyword">private</span> SingletonSupplier&lt;KeyGenerator&gt; keyGenerator = SingletonSupplier.of(SimpleKeyGenerator::<span class="keyword">new</span>);</span><br><span class="line">	<span class="comment">// 缓存操作解析类，根据缓存操作上下文确定缓存实例</span></span><br><span class="line">	<span class="keyword">private</span> SingletonSupplier&lt;CacheResolver&gt; cacheResolver;</span><br><span class="line">	<span class="comment">// 自动注入bean工厂</span></span><br><span class="line">	<span class="keyword">private</span> BeanFactory beanFactory;</span><br><span class="line">	<span class="comment">// 标识 是否初始化，在 SmartInitializingSingleton.afterSingletonsInstantiated 声明周期函数中置为 true</span></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">boolean</span> initialized = <span class="keyword">false</span>;</span><br><span class="line">	<span class="comment">// </span></span><br><span class="line">	<span class="function"><span class="keyword">protected</span> Object <span class="title">execute</span><span class="params">(CacheOperationInvoker invoker, Object target, Method method, Object[] args)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">if</span> (<span class="keyword">this</span>.initialized) &#123;</span><br><span class="line">			<span class="comment">// 拿到代理类的最终目标类</span></span><br><span class="line">			Class&lt;?&gt; targetClass = getTargetClass(target);</span><br><span class="line">			<span class="comment">// 得到 AnnotationCacheOperationSource</span></span><br><span class="line">			CacheOperationSource cacheOperationSource = getCacheOperationSource();</span><br><span class="line">			<span class="keyword">if</span> (cacheOperationSource != <span class="keyword">null</span>) &#123;</span><br><span class="line">				<span class="comment">// 解析方法上的 缓存操作注解，封装为 CacheOperation</span></span><br><span class="line">				Collection&lt;CacheOperation&gt; operations = cacheOperationSource.getCacheOperations(method, targetClass);</span><br><span class="line">				<span class="keyword">if</span> (!CollectionUtils.isEmpty(operations)) &#123;</span><br><span class="line">					<span class="comment">// 相关操作封装为 CacheOperationContexts 上下文</span></span><br><span class="line">					<span class="keyword">return</span> execute(invoker, method,	<span class="keyword">new</span> CacheOperationContexts(operations, method, args, target, targetClass));</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> invoker.invoke();</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// </span></span><br><span class="line">	<span class="function"><span class="keyword">private</span> Object <span class="title">execute</span><span class="params">(<span class="keyword">final</span> CacheOperationInvoker invoker, Method method, CacheOperationContexts contexts)</span> </span>&#123;</span><br><span class="line">		<span class="comment">// 多线程同步调用</span></span><br><span class="line">		<span class="keyword">if</span> (contexts.isSynchronized()) &#123;</span><br><span class="line">			CacheOperationContext context = contexts.get(CacheableOperation.class).iterator().next();</span><br><span class="line">			<span class="comment">// 计算 condition 表达式 是否通过</span></span><br><span class="line">			<span class="keyword">if</span> (isConditionPassing(context, CacheOperationExpressionEvaluator.NO_RESULT)) &#123;</span><br><span class="line">				<span class="comment">// 生成缓存key</span></span><br><span class="line">				Object key = generateKey(context, CacheOperationExpressionEvaluator.NO_RESULT);</span><br><span class="line">				<span class="comment">// 通过 CacheResolver 解析缓存，默认使用注解配置的 cacheNames 通过 CacheManager.getCache 得到</span></span><br><span class="line">				Cache cache = context.getCaches().iterator().next();</span><br><span class="line">				<span class="keyword">try</span> &#123;</span><br><span class="line">					<span class="comment">// invoker 执行原始方法，得到返回值，调用具体的 Cache 实现缓存操作，同步操作由具体实现负责</span></span><br><span class="line">					<span class="comment">// 方法返回值支持 Optional</span></span><br><span class="line">					<span class="keyword">return</span> wrapCacheValue(method, handleSynchronizedGet(invoker, key, cache));</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">catch</span> (Cache.ValueRetrievalException ex) &#123;</span><br><span class="line">					<span class="comment">// Directly propagate ThrowableWrapper from the invoker,</span></span><br><span class="line">					<span class="comment">// or potentially also an IllegalArgumentException etc.</span></span><br><span class="line">					ReflectionUtils.rethrowRuntimeException(ex.getCause());</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> &#123;</span><br><span class="line">				<span class="comment">// 无需缓存，直接调用底层方法</span></span><br><span class="line">				<span class="keyword">return</span> invokeOperation(invoker);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 执行方法调用前的缓存清理， CacheEvict 注解 beforeInvocation 设置为 true</span></span><br><span class="line">		processCacheEvicts(contexts.get(CacheEvictOperation.class), <span class="keyword">true</span>, CacheOperationExpressionEvaluator.NO_RESULT);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 检查是否命中 Cacheable 缓存</span></span><br><span class="line">		Cache.ValueWrapper cacheHit = findCachedItem(contexts.get(CacheableOperation.class));</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Cacheable 缓存未命中， 收集需要放置到缓存的操作</span></span><br><span class="line">		List&lt;CachePutRequest&gt; cachePutRequests = <span class="keyword">new</span> LinkedList&lt;&gt;();</span><br><span class="line">		<span class="keyword">if</span> (cacheHit == <span class="keyword">null</span>) &#123;</span><br><span class="line">			collectPutRequests(contexts.get(CacheableOperation.class), CacheOperationExpressionEvaluator.NO_RESULT, cachePutRequests);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		Object cacheValue;</span><br><span class="line">		Object returnValue;</span><br><span class="line">		<span class="comment">// Cacheable 缓存命中， 并且没有生效的 CachePut</span></span><br><span class="line">		<span class="keyword">if</span> (cacheHit != <span class="keyword">null</span> &amp;&amp; !hasCachePut(contexts)) &#123;</span><br><span class="line">			<span class="comment">// 直接使用命中的缓存值</span></span><br><span class="line">			cacheValue = cacheHit.get();</span><br><span class="line">			returnValue = wrapCacheValue(method, cacheValue);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span> &#123;</span><br><span class="line">			<span class="comment">// 执行目标方法获得返回值</span></span><br><span class="line">			returnValue = invokeOperation(invoker);</span><br><span class="line">			cacheValue = unwrapReturnValue(returnValue);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 收集显示的 CachePut 操作</span></span><br><span class="line">		collectPutRequests(contexts.get(CachePutOperation.class), cacheValue, cachePutRequests);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 执行收集后的缓存放置操作</span></span><br><span class="line">		<span class="keyword">for</span> (CachePutRequest cachePutRequest : cachePutRequests) &#123;</span><br><span class="line">			cachePutRequest.apply(cacheValue);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 执行方法调用前的缓存清理， CacheEvict 注解 beforeInvocation 设置为 false</span></span><br><span class="line">		processCacheEvicts(contexts.get(CacheEvictOperation.class), <span class="keyword">false</span>, cacheValue);</span><br><span class="line"></span><br><span class="line">		<span class="keyword">return</span> returnValue;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="BeanFactoryCacheOperationSourceAdvisor"><a href="#BeanFactoryCacheOperationSourceAdvisor" class="headerlink" title="BeanFactoryCacheOperationSourceAdvisor"></a>BeanFactoryCacheOperationSourceAdvisor</h4><p>见名知意，通知器，提供BeanFactory能力，CacheOperationSource能力，通知器主要能力由<code>AbstractBeanFactoryPointcutAdvisor</code>提供，提供切面实现<code>CacheOperationSourcePointcut</code>，再委托给<code>AnnotationCacheOperationSource</code>解析是否存在缓存相关注解</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">BeanFactoryCacheOperationSourceAdvisor</span> <span class="keyword">extends</span> <span class="title">AbstractBeanFactoryPointcutAdvisor</span> </span>&#123;</span><br><span class="line">	<span class="comment">// 自动配置注入 AnnotationCacheOperationSource</span></span><br><span class="line">	<span class="keyword">private</span> CacheOperationSource cacheOperationSource;</span><br><span class="line">	<span class="comment">// 类似适配器的功能，提供切面操作</span></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">final</span> CacheOperationSourcePointcut pointcut = <span class="keyword">new</span> CacheOperationSourcePointcut() &#123;</span><br><span class="line">		<span class="meta">@Override</span></span><br><span class="line">		<span class="meta">@Nullable</span></span><br><span class="line">		<span class="function"><span class="keyword">protected</span> CacheOperationSource <span class="title">getCacheOperationSource</span><span class="params">()</span> </span>&#123;</span><br><span class="line">			<span class="keyword">return</span> cacheOperationSource;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;;</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setCacheOperationSource</span><span class="params">(CacheOperationSource cacheOperationSource)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">this</span>.cacheOperationSource = cacheOperationSource;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setClassFilter</span><span class="params">(ClassFilter classFilter)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">this</span>.pointcut.setClassFilter(classFilter);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> Pointcut <span class="title">getPointcut</span><span class="params">()</span> </span>&#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">this</span>.pointcut;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>AbstractBeanFactoryPointcutAdvisor</code>是一个<code>PointcutAdvisor</code>，由切面驱动的通知器，实现了<code>BeanFactoryAware</code>接口提供bean工厂的能力，允许将任何<code>Advice</code>配置为<code>BeanFactory</code>中的bean。指定bean的名称可以减少初始化的耦合，在切入点实际匹配之前不初始化<code>Advice</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">AbstractBeanFactoryPointcutAdvisor</span> <span class="keyword">extends</span> <span class="title">AbstractPointcutAdvisor</span> <span class="keyword">implements</span> <span class="title">BeanFactoryAware</span> </span>&#123;</span><br><span class="line">	<span class="comment">// 通知bean的名称，用于从 BeanFactory 中获取通知</span></span><br><span class="line">	<span class="keyword">private</span> String adviceBeanName;</span><br><span class="line">	<span class="comment">// BeanFactoryAware 提供</span></span><br><span class="line">	<span class="keyword">private</span> BeanFactory beanFactory;</span><br><span class="line">	<span class="comment">// 通知</span></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">transient</span> <span class="keyword">volatile</span> Advice advice;</span><br><span class="line">	<span class="comment">// advice 的监视器对象，用于同步</span></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">transient</span> <span class="keyword">volatile</span> Object adviceMonitor = <span class="keyword">new</span> Object();</span><br><span class="line"></span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * 直接指定通知实例，使用时无需从bean工厂查找</span></span><br><span class="line"><span class="comment">	 * 自动配置直接设置 CacheInterceptor</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setAdvice</span><span class="params">(Advice advice)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">synchronized</span> (<span class="keyword">this</span>.adviceMonitor) &#123;</span><br><span class="line">			<span class="keyword">this</span>.advice = advice;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// 自动配置直接获取 CacheInterceptor</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> Advice <span class="title">getAdvice</span><span class="params">()</span> </span>&#123;</span><br><span class="line">		Advice advice = <span class="keyword">this</span>.advice;</span><br><span class="line">		<span class="keyword">if</span> (advice != <span class="keyword">null</span>) &#123;</span><br><span class="line">			<span class="keyword">return</span> advice;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">//下略。。。</span></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h2 id="Spring-Boot-自动配置"><a href="#Spring-Boot-自动配置" class="headerlink" title="Spring Boot 自动配置"></a>Spring Boot 自动配置</h2><p><code>Spring Boot</code>自动配置文件<code>spring-boot-autoconfigure-x.x.x.jar</code>中的 META-INF\spring.factories 可以找到缓存自动配置类<code>CacheAutoConfiguration</code>，在使用<code>@EnableCaching</code>注解后<br>开启</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration(proxyBeanMethods = false)</span></span><br><span class="line"><span class="meta">@ConditionalOnClass(CacheManager.class)</span></span><br><span class="line"><span class="meta">@ConditionalOnBean(CacheAspectSupport.class)</span></span><br><span class="line"><span class="meta">@ConditionalOnMissingBean(value = CacheManager.class, name = &quot;cacheResolver&quot;)</span></span><br><span class="line"><span class="meta">@EnableConfigurationProperties(CacheProperties.class)</span></span><br><span class="line"><span class="meta">@AutoConfigureAfter(&#123; CouchbaseDataAutoConfiguration.class, HazelcastAutoConfiguration.class, HibernateJpaAutoConfiguration.class, RedisAutoConfiguration.class &#125;)</span></span><br><span class="line"><span class="meta">@Import(&#123; CacheConfigurationImportSelector.class, CacheManagerEntityManagerFactoryDependsOnPostProcessor.class &#125;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CacheAutoConfiguration</span> </span>&#123;</span><br><span class="line">	<span class="comment">// 自定义 CacheManager</span></span><br><span class="line">	<span class="meta">@Bean</span></span><br><span class="line">	<span class="meta">@ConditionalOnMissingBean</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> CacheManagerCustomizers <span class="title">cacheManagerCustomizers</span><span class="params">(ObjectProvider&lt;CacheManagerCustomizer&lt;?&gt;&gt; customizers)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">new</span> CacheManagerCustomizers(customizers.orderedStream().collect(Collectors.toList()));</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// 确保 CacheManager 存在，如不存在提供可读性更好的异常提示</span></span><br><span class="line">	<span class="meta">@Bean</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> CacheManagerValidator <span class="title">cacheAutoConfigurationValidator</span><span class="params">(CacheProperties cacheProperties, ObjectProvider&lt;CacheManager&gt; cacheManager)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">new</span> CacheManagerValidator(cacheProperties, cacheManager);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// Spring data jpa 环境下，声明实体管理器工厂 依赖 cacheManager</span></span><br><span class="line">	<span class="meta">@ConditionalOnClass(LocalContainerEntityManagerFactoryBean.class)</span></span><br><span class="line">	<span class="meta">@ConditionalOnBean(AbstractEntityManagerFactoryBean.class)</span></span><br><span class="line">	<span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">CacheManagerEntityManagerFactoryDependsOnPostProcessor</span> <span class="keyword">extends</span> <span class="title">EntityManagerFactoryDependsOnPostProcessor</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">		CacheManagerEntityManagerFactoryDependsOnPostProcessor() &#123;</span><br><span class="line">			<span class="keyword">super</span>(<span class="string">&quot;cacheManager&quot;</span>);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	<span class="comment">// 导入各种缓存类型对应的配置类</span></span><br><span class="line">	<span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">CacheConfigurationImportSelector</span> <span class="keyword">implements</span> <span class="title">ImportSelector</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">		<span class="meta">@Override</span></span><br><span class="line">		<span class="keyword">public</span> String[] selectImports(AnnotationMetadata importingClassMetadata) &#123;</span><br><span class="line">			CacheType[] types = CacheType.values();</span><br><span class="line">			String[] imports = <span class="keyword">new</span> String[types.length];</span><br><span class="line">			<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; types.length; i++) &#123;</span><br><span class="line">				imports[i] = CacheConfigurations.getConfigurationClass(types[i]);</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">return</span> imports;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Java</category>
        <category>Spring</category>
        <category>Spring Cache</category>
      </categories>
      <tags>
        <tag>Spring</tag>
        <tag>Cache</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Security 学习笔记</title>
    <url>/2021/04/04/Spring-Security/</url>
    <content><![CDATA[<h1 id="Spring-Security-概览"><a href="#Spring-Security-概览" class="headerlink" title="Spring Security 概览"></a>Spring Security 概览</h1><h2 id="安全框架主要做了两件事："><a href="#安全框架主要做了两件事：" class="headerlink" title="安全框架主要做了两件事："></a>安全框架主要做了两件事：</h2><p>authentication (你是谁?)<br>authorization (你能做什么?)</p>
<h3 id="Web"><a href="#Web" class="headerlink" title="Web"></a>Web</h3><h4 id="Servlet"><a href="#Servlet" class="headerlink" title="Servlet"></a>Servlet</h4><p><code>Spring Security</code>的<code>Servlet</code>支持基于 <code>Servlet Filter</code></p>
<p><code>Spring</code>提供了一个<code>Filter</code>接口实现<code>DelegatingFilterProxy</code>来链接<code>Servlet</code>容器和<code>Spring</code>容器，它可以通过标准<code>Servlet</code>容器机制注册，并将实际处理委托给<code>Spring</code>容器中的<code>Bean</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doFilter</span><span class="params">(ServletRequest request, ServletResponse response, FilterChain filterChain)</span></span></span><br><span class="line"><span class="function">		<span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// Lazily initialize the delegate if necessary.</span></span><br><span class="line">	<span class="comment">// 懒加载机制延迟初始化 delegate实例变量使用volatile修饰</span></span><br><span class="line">	Filter delegateToUse = <span class="keyword">this</span>.delegate;</span><br><span class="line">	<span class="keyword">if</span> (delegateToUse == <span class="keyword">null</span>) &#123;</span><br><span class="line">		<span class="keyword">synchronized</span> (<span class="keyword">this</span>.delegateMonitor) &#123;</span><br><span class="line">			delegateToUse = <span class="keyword">this</span>.;</span><br><span class="line">			<span class="keyword">if</span> (delegateToUse == <span class="keyword">null</span>) &#123;</span><br><span class="line">				<span class="comment">// 查找`WebApplicationContext`容器</span></span><br><span class="line">				WebApplicationContext wac = findWebApplicationContext();</span><br><span class="line">				<span class="keyword">if</span> (wac == <span class="keyword">null</span>) &#123;</span><br><span class="line">					<span class="keyword">throw</span> <span class="keyword">new</span> IllegalStateException(<span class="string">&quot;No WebApplicationContext found: &quot;</span> +</span><br><span class="line">							<span class="string">&quot;no ContextLoaderListener or DispatcherServlet registered?&quot;</span>);</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="comment">// 初始化委托类（从容器中取出Bean）</span></span><br><span class="line">				delegateToUse = initDelegate(wac);</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">this</span>.delegate = delegateToUse;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// 实际处理委托给 delegate</span></span><br><span class="line">	<span class="comment">// Let the delegate perform the actual doFilter operation.</span></span><br><span class="line">	invokeDelegate(delegateToUse, request, response, filterChain);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">invokeDelegate</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">		Filter delegate, ServletRequest request, ServletResponse response, FilterChain filterChain)</span></span></span><br><span class="line"><span class="function">		<span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line"></span><br><span class="line">	delegate.doFilter(request, response, filterChain);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="FilterChainProxy"><a href="#FilterChainProxy" class="headerlink" title="FilterChainProxy"></a>FilterChainProxy</h4><p><code>Spring Security</code>中实际处理的工作由<code>FilterChainProxy</code>完成</p>
<p><code>FilterChainProxy</code>是一个特殊的<code>Filter</code>，它将处理逻辑再次委托给<code>SecurityFilterChain</code>，并通过<code>DelegatingFilterProxy</code>注册到<code>Servlet</code>容器</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doFilter</span><span class="params">(ServletRequest request, ServletResponse response, FilterChain chain)</span></span></span><br><span class="line"><span class="function">		<span class="keyword">throws</span> IOException, ServletException </span>&#123;</span><br><span class="line">	<span class="comment">//确保清理逻辑不重复</span></span><br><span class="line">	<span class="keyword">boolean</span> clearContext = request.getAttribute(FILTER_APPLIED) == <span class="keyword">null</span>;</span><br><span class="line">	<span class="keyword">if</span> (!clearContext) &#123;</span><br><span class="line">		doFilterInternal(request, response, chain);</span><br><span class="line">		<span class="keyword">return</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">try</span> &#123;</span><br><span class="line">		request.setAttribute(FILTER_APPLIED, Boolean.TRUE);</span><br><span class="line">		doFilterInternal(request, response, chain);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">catch</span> (RequestRejectedException ex) &#123;</span><br><span class="line">		<span class="keyword">this</span>.requestRejectedHandler.handle((HttpServletRequest) request, (HttpServletResponse) response, ex);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">finally</span> &#123;</span><br><span class="line">		<span class="comment">//清除上下文</span></span><br><span class="line">		SecurityContextHolder.clearContext();</span><br><span class="line">		request.removeAttribute(FILTER_APPLIED);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//内部处理逻辑</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">doFilterInternal</span><span class="params">(ServletRequest request, ServletResponse response, FilterChain chain)</span></span></span><br><span class="line"><span class="function">		<span class="keyword">throws</span> IOException, ServletException </span>&#123;</span><br><span class="line">	FirewalledRequest firewallRequest = <span class="keyword">this</span>.firewall.getFirewalledRequest((HttpServletRequest) request);</span><br><span class="line">	HttpServletResponse firewallResponse = <span class="keyword">this</span>.firewall.getFirewalledResponse((HttpServletResponse) response);</span><br><span class="line">	List&lt;Filter&gt; filters = getFilters(firewallRequest);</span><br><span class="line">	<span class="keyword">if</span> (filters == <span class="keyword">null</span> || filters.size() == <span class="number">0</span>) &#123;</span><br><span class="line">		<span class="keyword">if</span> (logger.isTraceEnabled()) &#123;</span><br><span class="line">			logger.trace(LogMessage.of(() -&gt; <span class="string">&quot;No security for &quot;</span> + requestLine(firewallRequest)));</span><br><span class="line">		&#125;</span><br><span class="line">		firewallRequest.reset();</span><br><span class="line">		chain.doFilter(firewallRequest, firewallResponse);</span><br><span class="line">		<span class="keyword">return</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">		logger.debug(LogMessage.of(() -&gt; <span class="string">&quot;Securing &quot;</span> + requestLine(firewallRequest)));</span><br><span class="line">	&#125;</span><br><span class="line">	VirtualFilterChain virtualFilterChain = <span class="keyword">new</span> VirtualFilterChain(firewallRequest, chain, filters);</span><br><span class="line">	virtualFilterChain.doFilter(firewallRequest, firewallResponse);</span><br><span class="line">&#125;</span><br><span class="line">   <span class="comment">//从内部的过滤器中匹配</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> List&lt;Filter&gt; <span class="title">getFilters</span><span class="params">(HttpServletRequest request)</span> </span>&#123;</span><br><span class="line">	<span class="keyword">int</span> count = <span class="number">0</span>;</span><br><span class="line">	<span class="keyword">for</span> (SecurityFilterChain chain : <span class="keyword">this</span>.filterChains) &#123;</span><br><span class="line">		<span class="keyword">if</span> (logger.isTraceEnabled()) &#123;</span><br><span class="line">			logger.trace(LogMessage.format(<span class="string">&quot;Trying to match request against %s (%d/%d)&quot;</span>, chain, ++count,</span><br><span class="line">					<span class="keyword">this</span>.filterChains.size()));</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">// 查找到第一个符合条件的`SecurityFilterChain`</span></span><br><span class="line">		<span class="keyword">if</span> (chain.matches(request)) &#123;</span><br><span class="line">			<span class="keyword">return</span> chain.getFilters();</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="SecurityFilterChain"><a href="#SecurityFilterChain" class="headerlink" title="SecurityFilterChain"></a>SecurityFilterChain</h4><p><code>SpringBoot</code>环境会自动注入一个默认的<code>SecurityFilterChain</code>，包含多个默认的<code>Filter</code>，通过<code>SecurityConfigurer</code>接口配置，可以通过自定义<code>WebSecurityConfigurerAdapter</code>或<code>SecurityFilterChain</code>来替换默认的<code>SecurityFilterChain</code>。</p>
<h3 id="Authentication"><a href="#Authentication" class="headerlink" title="Authentication"></a>Authentication</h3><h4 id="安全架构组件"><a href="#安全架构组件" class="headerlink" title="安全架构组件"></a>安全架构组件</h4><h5 id="SecurityContextHolder"><a href="#SecurityContextHolder" class="headerlink" title="SecurityContextHolder"></a>SecurityContextHolder</h5><p>认证模型的核心，存储认证信息<code>SecurityContext</code>，拥有多种存储策略，默认使用<code>ThreadLocal</code>存储。<code>FilterChainProxy</code>确保处理完成会清理存储。 </p>
<h5 id="SecurityContext"><a href="#SecurityContext" class="headerlink" title="SecurityContext"></a>SecurityContext</h5><p>存储认证信息 Authentication 的实体。  </p>
<h5 id="Authentication-1"><a href="#Authentication-1" class="headerlink" title="Authentication"></a>Authentication</h5><p>存储用于认证的信息，包括 ：<code>principal</code> （主体），<code>credentials</code>（凭证），<code>authorities</code>（权限），例如<code>UsernamePasswordAuthenticationToken</code></p>
<h5 id="GrantedAuthority"><a href="#GrantedAuthority" class="headerlink" title="GrantedAuthority"></a>GrantedAuthority</h5><p>授予主体的权限，<code>Authentication.getAuthorities()</code>方法提供<code>GrantedAuthority</code>权限的集合，通常可以用来表示角色。<br>例如基于用户名、密码的认证，使用<code>UserDetailsService</code>加载权限集合</p>
<h5 id="AuthenticationManager"><a href="#AuthenticationManager" class="headerlink" title="AuthenticationManager"></a>AuthenticationManager</h5><p>框架用于认证的策略接口，只有一个方法，对入参 Authentication 进行认证，认证失败抛出异常，无法认证返回null</p>
<h5 id="ProviderManager"><a href="#ProviderManager" class="headerlink" title="ProviderManager"></a>ProviderManager</h5><p><code>AuthenticationManager</code>的扩展，增加了一个<code>supports</code>方法，允许调用者询问是否支持给定<code>Authentication</code>类型的认证。<code>ProviderManager</code>持有<code>AuthenticationProvider</code>的集合组成的认证链路用以支持多种认证机制，其中一个认证通过，认证便成功。<br>通常在应用中受保护的资源会分为多个类别或分组，每个组应用一个<code>ProviderManager</code>，并且共享一个<code>parent</code>，<code>parent</code>用于处理公共的认证逻辑，作为所有<code>providers</code>的备用方案</p>
<h5 id="AuthenticationProvider"><a href="#AuthenticationProvider" class="headerlink" title="AuthenticationProvider"></a>AuthenticationProvider</h5><p>用于执行具体<code>Authentication</code>类型的认证，例如<code>DaoAuthenticationProvider</code>支持基于用户名、密码的认证</p>
<h5 id="Request-Credentials-with-AuthenticationEntryPoint"><a href="#Request-Credentials-with-AuthenticationEntryPoint" class="headerlink" title="Request Credentials with AuthenticationEntryPoint"></a>Request Credentials with AuthenticationEntryPoint</h5><p><code>AuthenticationEntryPoint</code>用于响应客户端请求凭证，例如重定向至登录页，返回<code>WWW-Authenticate</code>响应头</p>
<h5 id="AbstractAuthenticationProcessingFilter"><a href="#AbstractAuthenticationProcessingFilter" class="headerlink" title="AbstractAuthenticationProcessingFilter"></a>AbstractAuthenticationProcessingFilter</h5><p>认证处理的拦截器，从<code>HttpServletRequest</code>中取得用于认证的信息，封装为<code>Authentication</code>（例如：<code>UsernamePasswordAuthenticationToken</code>）。然后调用<code>AuthenticationManager</code>进行认证。</p>
<p>认证成功，调用<code>SessionAuthenticationStrategy</code>处理登录成功的<code>Session</code>信息，保存认证信息到<code>SecurityContextHolder</code>，随后<code>SecurityContextPersistenceFilter</code>保存<code>SecurityContext</code>至<code>HttpSession</code>。调用<code>RememberMeServices.loginSuccess</code>，发布<code>InteractiveAuthenticationSuccessEvent</code>事件，调用<code>AuthenticationSuccessHandler</code>。</p>
<p>认证失败，清理<code>SecurityContextHolder</code>，调用<code>RememberMeServices.loginFail</code>，调用<code>AuthenticationFailureHandler</code>。</p>
<h5 id="Session-Management"><a href="#Session-Management" class="headerlink" title="Session Management"></a>Session Management</h5><p><code>HTTP session</code>的管理主要由<code>SessionManagementFilter</code>和<code>SessionAuthenticationStrategy</code>两个接口联合完成，通常包括防御会话固定攻击，指定会话超时时间，限制单个用户并发会话数等。</p>
<h5 id="Anonymous-Authentication"><a href="#Anonymous-Authentication" class="headerlink" title="Anonymous Authentication"></a>Anonymous Authentication</h5><p>匿名认证，<code>Spring Security</code>提供了一种便捷的方式，对于未认证用户的行为进行统一管理</p>
<h4 id="认证机制"><a href="#认证机制" class="headerlink" title="认证机制"></a>认证机制</h4><h5 id="基于用户名、密码的认证"><a href="#基于用户名、密码的认证" class="headerlink" title="基于用户名、密码的认证"></a>基于用户名、密码的认证</h5><p><code>Spring Security</code>提供了如下内建的机制来获取用户名、密码</p>
<ul>
<li>Form Login</li>
<li>Basic Authentication</li>
<li>Digest Authentication</li>
</ul>
<p>另外有如下的存储机制，可以和以上的获取机制任意组合</p>
<ul>
<li>简单的基于内存存储 In-Memory Authentication</li>
<li>基于数据库的 JDBC Authentication</li>
<li>自定义存储 UserDetailsService</li>
<li> LDAP Authentication<h6 id="表单登录"><a href="#表单登录" class="headerlink" title="表单登录"></a>表单登录</h6></li>
</ul>
<ol>
<li>未认证的用户登录访问敏感资源<code>/private</code></li>
<li><code>FilterSecurityInterceptor</code>拦截请求，抛出<code>AccessDeniedException</code></li>
<li><code>ExceptionTranslationFilter</code>拦截异常，重定向请求</li>
<li>浏览器请求登录页</li>
<li>登录页提交用户名密码</li>
<li><code>UsernamePasswordAuthenticationFilter</code>拦截请求，从<code>HttpServletRequest</code>提取信息创建<code>UsernamePasswordAuthenticationToken</code></li>
<li><code>AuthenticationManager</code>认证信息</li>
</ol>
<p>显示配置表单登录如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">configure</span><span class="params">(HttpSecurity http)</span> </span>&#123;</span><br><span class="line">    http</span><br><span class="line">        <span class="comment">// ...</span></span><br><span class="line">        .formLogin(withDefaults());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>可以自定义配置</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">configure</span><span class="params">(HttpSecurity http)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">    http</span><br><span class="line">        <span class="comment">// ...</span></span><br><span class="line">        .formLogin(form -&gt; form</span><br><span class="line">            .loginPage(<span class="string">&quot;/login&quot;</span>)</span><br><span class="line">            .permitAll()</span><br><span class="line">        );</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h6 id="Basic-HTTP-认证"><a href="#Basic-HTTP-认证" class="headerlink" title="Basic HTTP 认证"></a>Basic HTTP 认证</h6><p>流程和表单登录类似，<code>ExceptionTranslationFilter</code>返回<code>WWW-Authenticate</code>响应头，<code>BasicAuthenticationFilter</code>拦截请求，从请求头<code>Authorization</code>中提取信息创建<code>UsernamePasswordAuthenticationToken</code></p>
<p>显示配置</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">configure</span><span class="params">(HttpSecurity http)</span> </span>&#123;</span><br><span class="line">    http</span><br><span class="line">        <span class="comment">// ...</span></span><br><span class="line">        .httpBasic(withDefaults());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h6 id="JDBC-认证"><a href="#JDBC-认证" class="headerlink" title="JDBC 认证"></a>JDBC 认证</h6><p><code>JdbcDaoImpl</code>实现了<code>UserDetailsService</code>接口，使用<code>JDBC</code>认证用户名、密码。<code>JdbcUserDetailsManager</code>扩展了<code>JdbcDaoImpl</code>并实现了<code>UserDetailsManager</code>，来管理<code>UserDetails</code></p>
<h6 id="UserDetails"><a href="#UserDetails" class="headerlink" title="UserDetails"></a>UserDetails</h6><p>认证主体，<code>UserDetailsService</code>生成<code>UserDetails</code>，<code>DaoAuthenticationProvider</code>验证<code>UserDetails</code>并返回<code>Authentication</code></p>
<h6 id="UserDetailsService"><a href="#UserDetailsService" class="headerlink" title="UserDetailsService"></a>UserDetailsService</h6><p>加载用户数据的核心接口，由<code>DaoAuthenticationProvider</code>用来检索用户名、密码和其他属性以便认证，可以自定义检索逻辑，例如从数据库查询</p>
<h6 id="PasswordEncoder"><a href="#PasswordEncoder" class="headerlink" title="PasswordEncoder"></a>PasswordEncoder</h6><p><code>Spring Security</code>提供了<code>PasswordEncoder</code>接口存储密码，内置多种算法实现，如<code>bcrypt</code>，<code>pbkdf2</code>等</p>
<h6 id="DaoAuthenticationProvider"><a href="#DaoAuthenticationProvider" class="headerlink" title="DaoAuthenticationProvider"></a>DaoAuthenticationProvider</h6><p><code>AuthenticationProvider</code>接口的实现，使用<code>UserDetailsService</code>和<code>PasswordEncoder</code>认证用户名和密码。<br>认证流程大致描述如下：</p>
<ol>
<li>从获取用户名密码的拦截器（例如表单登录使用<code>UsernamePasswordAuthenticationFilter</code>）中解析请求信息，将认证所需信息封装到<code>UsernamePasswordAuthenticationToken</code>，交给<code>AuthenticationManager</code>进行认证</li>
<li><code>AuthenticationManager</code>的实现是<code>ProviderManager</code>,<code>ProviderManager</code>委托给<code>DaoAuthenticationProvider</code>执行</li>
<li><code>DaoAuthenticationProvider</code>通过<code>UserDetailsService</code>检索相关信息，得到<code>UserDetails</code></li>
<li><code>DaoAuthenticationProvider</code>使用<code>PasswordEncoder</code>验证<code>UserDetails</code></li>
<li>验证通过的<code>UserDetails</code>会被封装到<code>UsernamePasswordAuthenticationToken</code>，返回认证信息，进行过滤链后续处理<h3 id="Authorization"><a href="#Authorization" class="headerlink" title="Authorization"></a>Authorization</h3>鉴权是指验证用户访问系统资源的权力，它的前提是用户身份已经通过认证。<h4 id="框架结构"><a href="#框架结构" class="headerlink" title="框架结构"></a>框架结构</h4><h5 id="权限"><a href="#权限" class="headerlink" title="权限"></a>权限</h5>在<code>Authentication</code>中包含<code>GrantedAuthority</code>构成的集合，这些代表授予主体的权限。在<code>AuthenticationManager</code>认证过程中，保存到<code>Authentication</code>，在后续的鉴权过程中由<code>AccessDecisionManager</code>使用。<br>权限通常可以简单的使用字符串标识，比如角色名称，可以使用方法<code>getAuthority()</code>返回值精确标识。如果是更复杂的情况，可以自定义<code>AccessDecisionManager</code>支持<code>GrantedAuthority</code>的具体实现，并且方法<code>getAuthority()</code>必须返回<code>null</code>。<h5 id="前置处理"><a href="#前置处理" class="headerlink" title="前置处理"></a>前置处理</h5><code>Spring Security</code>提供了拦截机制来控制方法调用或者web请求的访问。<code>AccessDecisionManager</code>在<code>AbstractSecurityInterceptor</code>中访问安全对象之前被调用，来决定是否有权限访问某种资源。<h6 id="基于投票的AccessDecisionManager实现"><a href="#基于投票的AccessDecisionManager实现" class="headerlink" title="基于投票的AccessDecisionManager实现"></a>基于投票的AccessDecisionManager实现</h6><code>Spring Security</code>提供了一种投票机制，一组<code>AccessDecisionVoter</code>轮询决定授予，拒绝或者弃权。默认提供了三种投票机制的实现</li>
</ol>
<ul>
<li>ConsensusBased 票多胜出，参数可以控制全部弃权或票数相等的情况</li>
<li>AffirmativeBased 一票通过，参数可以控制全部弃权的情况</li>
<li>UnanimousBased 一票否决，参数可以控制全部弃权的情况<br><code>RoleVoter</code>是比较常用的<code>AccessDecisionVoter</code>实现，使用角色名称标识并带有<code>ROLE_</code>前缀，角色不匹配拒绝访问，没有<code>ROLE_</code>前缀则弃权。<h5 id="后置处理"><a href="#后置处理" class="headerlink" title="后置处理"></a>后置处理</h5>有些情况下，需要对安全对象的返回值做权限控制，使用<code>AOP</code>可以实现这一点，但是<code>Spring Security</code>提供了一种便捷的钩子来处理此类问题。<code>AfterInvocationManager</code>有唯一的实现<code>AfterInvocationProviderManager</code>，轮询一组<code>AfterInvocationProvider</code>，每个<code>provider</code>都可以修改返回的对象或者抛出拒绝访问的异常。<h5 id="角色分级"><a href="#角色分级" class="headerlink" title="角色分级"></a>角色分级</h5>一个常见的需求是，一个角色可能包含多个其他角色的权限，例如<code>系统管理员</code>和<code>普通用户</code>，系统管理员包含普通用户的所有权限，除了直接授权还可以使系统管理员包含普通用户的角色，也就是角色分级机制，使用<code>RoleHierarchyVoter</code>配置不同角色直接的包含关系。<h4 id="web请求认证过程"><a href="#web请求认证过程" class="headerlink" title="web请求认证过程"></a>web请求认证过程</h4></li>
</ul>
<ol>
<li><code>FilterSecurityInterceptor</code>从<code>SecurityContextHolder</code>中取出<code>Authentication</code></li>
<li>根据<code>HttpServletRequest</code>，<code>HttpServletResponse</code>，<code>FilterChain</code>创建<code>FilterInvocation</code></li>
<li><code>FilterInvocation</code>传递给<code>SecurityMetadataSource</code>获取<code>ConfigAttribute</code></li>
<li><code>Authentication</code>，<code>FilterInvocation</code>，<code>ConfigAttribute</code>s传递给<code>AccessDecisionManager</code><ul>
<li>如果授权被拒绝，则抛出<code>AccessDeniedException</code>，然后由<code>ExceptionTranslationFilter</code>处理。</li>
<li>如果允许访问，<code>FilterSecurityInterceptor</code>将后续访问<code>FilterChain</code>。<h4 id="基于表达式的访问控制"><a href="#基于表达式的访问控制" class="headerlink" title="基于表达式的访问控制"></a>基于表达式的访问控制</h4><code>Spring Security</code>使用<code>Spring EL</code>来支持基于表达式的访问控制。使用<code>SecurityExpressionRoot</code>作为根对象，以便提供内置表达式和对当前认证主体等值的访问。<br>例如<code>hasRole(String role)</code>，方法可在根对象中查看。<h5 id="web安全表达式"><a href="#web安全表达式" class="headerlink" title="web安全表达式"></a>web安全表达式</h5><code>web</code>环境中使用<code>WebSecurityExpressionRoot</code>，可以直接在表达式中使用<code>request</code>引用<code>HttpServletRequest</code>对象，此时使用<code>WebExpressionVoter</code>执行表达式鉴权。<br>还可以通过<code>Spring Bean</code>方式自定义权限控制<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">WebSecurity</span> </span>&#123;</span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">checkUserId</span><span class="params">(Authentication authentication, <span class="keyword">int</span> id)</span> </span>&#123;</span><br><span class="line">			...</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
通过@引用<code>Spring Bean</code>，通过#引用路径变量<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http</span><br><span class="line">    .authorizeRequests(authorize -&gt; authorize</span><br><span class="line">        .antMatchers(<span class="string">&quot;/user/&#123;userId&#125;/**&quot;</span>).access(<span class="string">&quot;@webSecurity.checkUserId(authentication,#userId)&quot;</span>)</span><br><span class="line">        ...</span><br><span class="line">    );</span><br></pre></td></tr></table></figure>
<h5 id="method安全表达式"><a href="#method安全表达式" class="headerlink" title="method安全表达式"></a>method安全表达式</h5>方法级别的安全使用一系列注解实现，根对象为<code>MethodSecurityExpressionRoot</code></li>
</ul>
</li>
</ol>
<ul>
<li><code>@PreAuthorize</code><br>方法实际执行前判断是否可以执行，同样可以引用方法入参，<code>DefaultSecurityParameterNameDiscoverer</code>用来发现入参的名称<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@PreAuthorize(&quot;hasPermission(#contact, &#x27;admin&#x27;)&quot;)</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">deletePermission</span><span class="params">(Contact contact, Sid recipient, Permission permission)</span></span>;</span><br></pre></td></tr></table></figure>
另外同样支持<code>Spring-EL</code>的各种特性<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@PreAuthorize(&quot;#contact.name == authentication.name&quot;)</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doSomething</span><span class="params">(Contact contact)</span></span>;</span><br></pre></td></tr></table></figure></li>
<li><code>@PostAuthorize</code><br>可用于在方法执行后校验，可通过<code>returnObject</code>访问方法返回值<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@PostAuthorize(&quot;returnObject.id%2==0&quot;)</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> User <span class="title">check</span><span class="params">(List&lt;Integer&gt; users)</span></span>;</span><br></pre></td></tr></table></figure></li>
<li><code>@PreFilter</code><br>可用于过滤集合类的入参，移除表达式结果为<code>false</code>的元素，多个集合可使用<code>filterTarget</code>指定，<code>filterObject</code>用来引用集合中当前元素<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@PreFilter(filterTarget=&quot;users&quot;, value=&quot;filterObject%2==0&quot;)</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">remain</span><span class="params">(List&lt;Integer&gt; users, List&lt;String&gt; usernames)</span></span>;</span><br></pre></td></tr></table></figure></li>
<li><code>@PostFilter</code><br>可用于过滤集合类的执行结果，移除表达式结果为<code>false</code>的元素，<code>filterObject</code>用来引用集合中当前元素<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@PreAuthorize(&quot;hasRole(&#x27;USER&#x27;)&quot;)</span></span><br><span class="line"><span class="meta">@PostFilter(&quot;hasPermission(filterObject, &#x27;read&#x27;) or hasPermission(filterObject, &#x27;admin&#x27;)&quot;)</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> List&lt;Contact&gt; <span class="title">getAll</span><span class="params">()</span></span>;</span><br></pre></td></tr></table></figure>
为了代码可读性，可以定义元注解，对于复杂的表达式的重用特别便捷<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Retention(RetentionPolicy.RUNTIME)</span></span><br><span class="line"><span class="meta">@PreAuthorize(&quot;#contact.name == authentication.name&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> ContactPermission &#123;&#125;</span><br></pre></td></tr></table></figure>
<h4 id="安全对象实现"><a href="#安全对象实现" class="headerlink" title="安全对象实现"></a>安全对象实现</h4><code>Spring Security</code>中的安全对象指所有需要被安全限制的对象，如方法调用，web请求等。通过拦截器机制实现安全控制，有动态和静态两种方式</li>
</ul>
<ol>
<li>动态的是通过<code>MethodSecurityInterceptor</code>实现的，基于AOP联盟，依赖<code>Spring</code>应用上下文通过代理实现织入，主要用于服务层安全</li>
<li><code>AspectJSecurityInterceptor</code>通过<code>AspectJ</code>编译器实现静态织入，主要用于领域对象实例安全<h4 id="方法安全"><a href="#方法安全" class="headerlink" title="方法安全"></a>方法安全</h4>从2.0版本以来，<code>Spring Security</code>极大的提高了对服务层方法安全的支持，它支持<code>JSR-250</code>注释和<code>@Secured</code>注释。从3.0开始，还可以使用基于表达式的注解。可以使用<code>intercept-methods</code>装饰单个<code>Bean</code>，或者可以使用<code>AspectJ</code>风格的切面保护多个<code>Bean</code>。<h5 id="注解支持"><a href="#注解支持" class="headerlink" title="注解支持"></a>注解支持</h5>在<code>@Configuration</code>注解的实例上使用<code>@EnableGlobalMethodSecurity</code>开启注解支持。<br>注解在<code>Spring Bean</code>中使用才能生效–基于AOP，不在<code>Spring</code>上下文中的方法可使用<code>AspectJ</code>。<br>指定<code>prePostEnabled</code>属性为<code>true</code>可以开启<code>pre post</code>系列注解，<br>指定<code>securedEnabled</code>属性为<code>true</code>可以开启<code>Secured</code>注解，<br>指定<code>jsr250Enabled</code>属性为<code>true</code>可以开启<code>JSR-250</code>系列注解。</li>
</ol>
<p><strong>注意</strong>：可以同时开启多种注解方案在同一应用中，但是在同一个类中只有一种类型注解可以生效。</p>
<p><code>@EnableGlobalMethodSecurity</code>配置类可继承<code>GlobalMethodSecurityConfiguration</code>自定义设置 。</p>
<h4 id="领域对象安全"><a href="#领域对象安全" class="headerlink" title="领域对象安全"></a>领域对象安全</h4><h5 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h5><p>相比于web和方法安全，实际应用中通常会定义更复杂的权限控制，而且安全决策需要同时包括<code>who</code>（身份验证）、<code>where</code>（方法调用）和<code>what</code>（领域对象）。<br>考虑一个常见的需求，有两种角色，卖家和客户。卖家可以访问客户的数据，客户之间也有部分信息可以互相查看，使用<code>Spring Security</code>，通常有以下几种方案。</p>
<ol>
<li>业务代码中直接控制权限访问，例如从<code>SecurityContextHolder</code>中取得认证数据，查询数据库获得领域对象信息。</li>
<li>实现一个<code>AccessDecisionVoter</code>，访问<code>Authentication</code>中的权限数据，自定义访问其他领域对象的权限。</li>
<li>实现一个<code>AccessDecisionVoter</code>，在投票器中查询数据库获得领域对象信息，从而做出鉴权决策。</li>
</ol>
<p>第一种方案，业务代码强关联，单元测试困难，领域对象的鉴权不能复用。<br>第二种方案，如果权限数据量很大，加载权限消耗的内存和时间可能无法接受。<br>最后一种没有前面描述的问题，并且实现了关注点分离，但是它仍然是低效的，因为在投票器和业务方法中都检索数据库访问领域对象信息，这显然不合理。<br>另外，上面的方案都需要从头开始编写权限控制列表（ACL）持久化和业务代码。<br><code>Spring Security</code>为我们提供了一种便捷的域对象安全管理策略。</p>
<h5 id="核心概念"><a href="#核心概念" class="headerlink" title="核心概念"></a>核心概念</h5><p><code>Spring Security</code>领域对象安全功能以<code>ACL</code>的概念为核心，系统中每一个领域对象实例都拥有各自的<code>ACL</code>，记录哪些人可以哪些人不可以使用该领域对象。<br>基于这一点，<code>Spring Security</code>为应用程序提供了三个主要的<code>ACL</code>相关功能：</p>
<ol>
<li>高效检索所有领域对象ACL条目（并且修改他们）的方法</li>
<li>在方法调用之前，确保给定主体被允许处理对象方法</li>
<li>在方法调用之后，确保给定主体被允许处理对象或者返回值的方法</li>
</ol>
<p>ACL模块的主要功能之一是提供检索ACL的高性能方法，这个ACL存储库的功能十分重要，因为系统中的每个领域对象都有可能访问多个ACL条目，并且每个ACL可能从其他ACL继承，组成类似树一样的结构。<br>ACL功能经过仔细设计，以提供高性能检索，可拔插缓存，死锁最小化数据库更新，独立于ORM框架（直接使用JDBC），适当的封装，透明的数据库更新等特性。</p>
<h3 id="CSRF"><a href="#CSRF" class="headerlink" title="CSRF"></a>CSRF</h3><p>Cross Site Request Forgery 跨站请求伪造，Spring Security默认启用csrf保护，CsrfFilter针对（”GET”, “HEAD”, “TRACE”, “OPTIONS”）以外的方法生效，LazyCsrfTokenRepository将 CsrfToken 值存储在 HttpSession 中，并指定前端把CsrfToken 值放在名为“_csrf”的请求参数或名为“X-CSRF-TOKEN”的请求头字段里</p>
<h3 id="异常处理"><a href="#异常处理" class="headerlink" title="异常处理"></a>异常处理</h3><p><code>ExceptionTranslationFilter</code>用于处理<code>AuthenticationException</code>（未认证）和<code>AccessDeniedException</code>（拒绝访问） ，分别由<code>AuthenticationEntryPoint</code>，<code>AccessDeniedHandler</code>接口处理，可以自定义处理逻辑</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SecurityConfig</span> <span class="keyword">extends</span> <span class="title">WebSecurityConfigurerAdapter</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">configure</span><span class="params">(HttpSecurity httpSecurity)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">		<span class="comment">//添加自定义未授权和未登录结果返回</span></span><br><span class="line">        httpSecurity.exceptionHandling().accessDeniedHandler(restAccessDeniedHandler).authenticationEntryPoint(restAuthenticationEntryPoint);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="Java-Configuration"><a href="#Java-Configuration" class="headerlink" title="Java Configuration"></a>Java Configuration</h2><h3 id="EnableWebSecurity"><a href="#EnableWebSecurity" class="headerlink" title="@EnableWebSecurity"></a>@EnableWebSecurity</h3><p><code>@EnableWebSecurity</code>注解启用<code>Spring Security</code>（<code>Spring Boot</code>自动开启）</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Retention(RetentionPolicy.RUNTIME)</span></span><br><span class="line"><span class="meta">@Target(ElementType.TYPE)</span></span><br><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="meta">@Import(&#123; WebSecurityConfiguration.class, SpringWebMvcImportSelector.class, OAuth2ImportSelector.class,</span></span><br><span class="line"><span class="meta">		HttpSecurityConfiguration.class &#125;)</span></span><br><span class="line"><span class="meta">@EnableGlobalAuthentication</span></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> EnableWebSecurity &#123;</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="keyword">boolean</span> <span class="title">debug</span><span class="params">()</span> <span class="keyword">default</span> <span class="keyword">false</span></span>;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>引入了<code>WebSecurityConfiguration</code>自动配置类，默认配置了一个<code>SecurityFilterChain</code>，由<code>webSecurity</code>构建</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Creates the Spring Security Filter Chain</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@return</span> the &#123;<span class="doctag">@link</span> Filter&#125; that represents the security filter chain</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@throws</span> Exception</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Bean(name = AbstractSecurityWebApplicationInitializer.DEFAULT_FILTER_NAME)</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> Filter <span class="title">springSecurityFilterChain</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">	<span class="keyword">boolean</span> hasConfigurers = <span class="keyword">this</span>.webSecurityConfigurers != <span class="keyword">null</span> &amp;&amp; !<span class="keyword">this</span>.webSecurityConfigurers.isEmpty();</span><br><span class="line">	<span class="keyword">boolean</span> hasFilterChain = !<span class="keyword">this</span>.securityFilterChains.isEmpty();</span><br><span class="line">	<span class="comment">// SecurityConfigurer SecurityFilterChain 二选一</span></span><br><span class="line">	Assert.state(!(hasConfigurers &amp;&amp; hasFilterChain),</span><br><span class="line">			<span class="string">&quot;Found WebSecurityConfigurerAdapter as well as SecurityFilterChain. Please select just one.&quot;</span>);</span><br><span class="line">	<span class="keyword">if</span> (!hasConfigurers &amp;&amp; !hasFilterChain) &#123;</span><br><span class="line">		<span class="comment">// 未配置的默认选项</span></span><br><span class="line">		WebSecurityConfigurerAdapter adapter = <span class="keyword">this</span>.objectObjectPostProcessor</span><br><span class="line">				.postProcess(<span class="keyword">new</span> WebSecurityConfigurerAdapter() &#123;</span><br><span class="line">				&#125;);</span><br><span class="line">		<span class="keyword">this</span>.webSecurity.apply(adapter);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">for</span> (SecurityFilterChain securityFilterChain : <span class="keyword">this</span>.securityFilterChains) &#123;</span><br><span class="line">		<span class="keyword">this</span>.webSecurity.addSecurityFilterChainBuilder(() -&gt; securityFilterChain);</span><br><span class="line">		<span class="keyword">for</span> (Filter filter : securityFilterChain.getFilters()) &#123;</span><br><span class="line">			<span class="keyword">if</span> (filter <span class="keyword">instanceof</span> FilterSecurityInterceptor) &#123;</span><br><span class="line">				<span class="keyword">this</span>.webSecurity.securityInterceptor((FilterSecurityInterceptor) filter);</span><br><span class="line">				<span class="keyword">break</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//可以实现 WebSecurityCustomizer 接口定制构建过程</span></span><br><span class="line">	<span class="keyword">for</span> (WebSecurityCustomizer customizer : <span class="keyword">this</span>.webSecurityCustomizers) &#123;</span><br><span class="line">		customizer.customize(<span class="keyword">this</span>.webSecurity);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//执行构建</span></span><br><span class="line">	<span class="keyword">return</span> <span class="keyword">this</span>.webSecurity.build();</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h3 id="WebSecurity"><a href="#WebSecurity" class="headerlink" title="WebSecurity"></a>WebSecurity</h3><p><code>webSecurity</code>的初始化及<code>WebSecurityConfigurerAdapter</code>的注入，初始化需要<code>ObjectPostProcessor</code>，这是一个对象处理器，通常用来处理<code>Aware</code>方法，<code>InitializingBean.afterPropertiesSet()</code>，<code> DisposableBean.destroy()</code> 方法，以便没有使用容器管理的对象支持<code>Spring</code>的一些周期函数。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Autowired(required = false)</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setFilterChainProxySecurityConfigurer</span><span class="params">(ObjectPostProcessor&lt;Object&gt; objectPostProcessor,</span></span></span><br><span class="line"><span class="function"><span class="params">		<span class="meta">@Value(&quot;#&#123;@autowiredWebSecurityConfigurersIgnoreParents.getWebSecurityConfigurers()&#125;&quot;)</span> List&lt;SecurityConfigurer&lt;Filter, WebSecurity&gt;&gt; webSecurityConfigurers)</span></span></span><br><span class="line"><span class="function">		<span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">	<span class="comment">//周期函数的处理</span></span><br><span class="line">	<span class="keyword">this</span>.webSecurity = objectPostProcessor.postProcess(<span class="keyword">new</span> WebSecurity(objectPostProcessor));</span><br><span class="line">	<span class="keyword">if</span> (<span class="keyword">this</span>.debugEnabled != <span class="keyword">null</span>) &#123;</span><br><span class="line">		<span class="keyword">this</span>.webSecurity.debug(<span class="keyword">this</span>.debugEnabled);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//支持Order接口排序或注解排序</span></span><br><span class="line">	webSecurityConfigurers.sort(AnnotationAwareOrderComparator.INSTANCE);</span><br><span class="line">	Integer previousOrder = <span class="keyword">null</span>;</span><br><span class="line">	Object previousConfig = <span class="keyword">null</span>;</span><br><span class="line">	<span class="keyword">for</span> (SecurityConfigurer&lt;Filter, WebSecurity&gt; config : webSecurityConfigurers) &#123;</span><br><span class="line">		Integer order = AnnotationAwareOrderComparator.lookupOrder(config);</span><br><span class="line">		<span class="keyword">if</span> (previousOrder != <span class="keyword">null</span> &amp;&amp; previousOrder.equals(order)) &#123;</span><br><span class="line">			<span class="keyword">throw</span> <span class="keyword">new</span> IllegalStateException(<span class="string">&quot;@Order on WebSecurityConfigurers must be unique. Order of &quot;</span> + order</span><br><span class="line">					+ <span class="string">&quot; was already used on &quot;</span> + previousConfig + <span class="string">&quot;, so it cannot be used on &quot;</span> + config + <span class="string">&quot; too.&quot;</span>);</span><br><span class="line">		&#125;</span><br><span class="line">		previousOrder = order;</span><br><span class="line">		previousConfig = config;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//SecurityConfigurer 接口用于定制构建的过程，有 init，configure 两个生命周期函数，通常使用它的子类 WebSecurityConfigurerAdapter</span></span><br><span class="line">	<span class="keyword">for</span> (SecurityConfigurer&lt;Filter, WebSecurity&gt; webSecurityConfigurer : webSecurityConfigurers) &#123;</span><br><span class="line">		<span class="keyword">this</span>.webSecurity.apply(webSecurityConfigurer);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">this</span>.webSecurityConfigurers = webSecurityConfigurers;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>WebSecurity</code>继承了<code>AbstractConfiguredSecurityBuilder&lt;Filter, WebSecurity&gt;</code>，<code>AbstractSecurityBuilder</code>，是一个<code>SecurityBuilder</code>，用于构建<code>FilterChainProxy</code>，可以使用<code>SecurityConfigurer</code>，<code>WebSecurityConfigurerAdapter</code>或<code>WebSecurityCustomizer</code>定制构建过程。</p>
<p><code>AbstractConfiguredSecurityBuilder</code>使用建造者模式，允许多个<code>SecurityConfigurer</code>策略对象配置不同的目标，分离构建过程，<br>使用模板方法定义构建过程，依次应用<code>SecurityConfigurer</code>的<code>init</code>，<code>configure</code>方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">AbstractConfiguredSecurityBuilder</span>&lt;<span class="title">O</span>, <span class="title">B</span> <span class="keyword">extends</span> <span class="title">SecurityBuilder</span>&lt;<span class="title">O</span>&gt;&gt;</span></span><br><span class="line"><span class="class">		<span class="keyword">extends</span> <span class="title">AbstractSecurityBuilder</span>&lt;<span class="title">O</span>&gt;</span>&#123;</span><br><span class="line">	<span class="comment">//模板方法			</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="function"><span class="keyword">protected</span> <span class="keyword">final</span> O <span class="title">doBuild</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">		<span class="keyword">synchronized</span> (<span class="keyword">this</span>.configurers) &#123;</span><br><span class="line">			<span class="keyword">this</span>.buildState = BuildState.INITIALIZING;</span><br><span class="line">			beforeInit();</span><br><span class="line">			init();</span><br><span class="line">			<span class="keyword">this</span>.buildState = BuildState.CONFIGURING;</span><br><span class="line">			beforeConfigure();</span><br><span class="line">			configure();</span><br><span class="line">			<span class="keyword">this</span>.buildState = BuildState.BUILDING;</span><br><span class="line">			O result = performBuild();</span><br><span class="line">			<span class="keyword">this</span>.buildState = BuildState.BUILT;</span><br><span class="line">			<span class="keyword">return</span> result;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//子类实现，在init方法之前执行</span></span><br><span class="line">	<span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">beforeInit</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//依次调用SecurityConfigurer.init</span></span><br><span class="line">	<span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">		Collection&lt;SecurityConfigurer&lt;O, B&gt;&gt; configurers = getConfigurers();</span><br><span class="line">		<span class="keyword">for</span> (SecurityConfigurer&lt;O, B&gt; configurer : configurers) &#123;</span><br><span class="line">			configurer.init((B) <span class="keyword">this</span>);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">for</span> (SecurityConfigurer&lt;O, B&gt; configurer : <span class="keyword">this</span>.configurersAddedInInitializing) &#123;</span><br><span class="line">			configurer.init((B) <span class="keyword">this</span>);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//子类实现，在configure方法之前执行</span></span><br><span class="line">	<span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">beforeConfigure</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//依次调用SecurityConfigurer.configure</span></span><br><span class="line">	<span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">configure</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">		Collection&lt;SecurityConfigurer&lt;O, B&gt;&gt; configurers = getConfigurers();</span><br><span class="line">		<span class="keyword">for</span> (SecurityConfigurer&lt;O, B&gt; configurer : configurers) &#123;</span><br><span class="line">			configurer.configure((B) <span class="keyword">this</span>);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//子类执行具体构建</span></span><br><span class="line">	<span class="function"><span class="keyword">protected</span> <span class="keyword">abstract</span> O <span class="title">performBuild</span><span class="params">()</span> <span class="keyword">throws</span> Exception</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>WebSecurity</code>使用<code>SecurityBuilder&lt;? extends SecurityFilterChain&gt;</code>定制构建，通常使用<code>WebSecurityConfigurerAdapter</code>，分别执行<code>SecurityFilterChain</code>列表的构建，每个<code>SecurityBuilder&lt;? extends SecurityFilterChain&gt;</code>构建一个<code>SecurityFilterChain</code>实例，多个实例构建为<code>FilterChainProxy</code>实例</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> Filter <span class="title">performBuild</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">	Assert.state(!<span class="keyword">this</span>.securityFilterChainBuilders.isEmpty(),</span><br><span class="line">			() -&gt; <span class="string">&quot;At least one SecurityBuilder&lt;? extends SecurityFilterChain&gt; needs to be specified. &quot;</span></span><br><span class="line">					+ <span class="string">&quot;Typically this is done by exposing a SecurityFilterChain bean &quot;</span></span><br><span class="line">					+ <span class="string">&quot;or by adding a @Configuration that extends WebSecurityConfigurerAdapter. &quot;</span></span><br><span class="line">					+ <span class="string">&quot;More advanced users can invoke &quot;</span> + WebSecurity.class.getSimpleName()</span><br><span class="line">					+ <span class="string">&quot;.addSecurityFilterChainBuilder directly&quot;</span>);</span><br><span class="line">	<span class="keyword">int</span> chainSize = <span class="keyword">this</span>.ignoredRequests.size() + <span class="keyword">this</span>.securityFilterChainBuilders.size();</span><br><span class="line">	List&lt;SecurityFilterChain&gt; securityFilterChains = <span class="keyword">new</span> ArrayList&lt;&gt;(chainSize);</span><br><span class="line">	<span class="comment">//不需要进行安全控制的部分</span></span><br><span class="line">	<span class="keyword">for</span> (RequestMatcher ignoredRequest : <span class="keyword">this</span>.ignoredRequests) &#123;</span><br><span class="line">		securityFilterChains.add(<span class="keyword">new</span> DefaultSecurityFilterChain(ignoredRequest));</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//分别执行构建</span></span><br><span class="line">	<span class="keyword">for</span> (SecurityBuilder&lt;? extends SecurityFilterChain&gt; securityFilterChainBuilder : <span class="keyword">this</span>.securityFilterChainBuilders) &#123;</span><br><span class="line">		securityFilterChains.add(securityFilterChainBuilder.build());</span><br><span class="line">	&#125;</span><br><span class="line">	FilterChainProxy filterChainProxy = <span class="keyword">new</span> FilterChainProxy(securityFilterChains);</span><br><span class="line">	<span class="comment">//设置防火墙</span></span><br><span class="line">	<span class="keyword">if</span> (<span class="keyword">this</span>.httpFirewall != <span class="keyword">null</span>) &#123;</span><br><span class="line">		filterChainProxy.setFirewall(<span class="keyword">this</span>.httpFirewall);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//设置请求拒绝异常处理类</span></span><br><span class="line">	<span class="keyword">if</span> (<span class="keyword">this</span>.requestRejectedHandler != <span class="keyword">null</span>) &#123;</span><br><span class="line">		filterChainProxy.setRequestRejectedHandler(<span class="keyword">this</span>.requestRejectedHandler);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//初始化</span></span><br><span class="line">	filterChainProxy.afterPropertiesSet();</span><br><span class="line"></span><br><span class="line">	Filter result = filterChainProxy;</span><br><span class="line">	<span class="keyword">if</span> (<span class="keyword">this</span>.debugEnabled) &#123;</span><br><span class="line">		<span class="keyword">this</span>.logger.warn(<span class="string">&quot;\n\n&quot;</span> + <span class="string">&quot;********************************************************************\n&quot;</span></span><br><span class="line">				+ <span class="string">&quot;**********        Security debugging is enabled.       *************\n&quot;</span></span><br><span class="line">				+ <span class="string">&quot;**********    This may include sensitive information.  *************\n&quot;</span></span><br><span class="line">				+ <span class="string">&quot;**********      Do not use in a production system!     *************\n&quot;</span></span><br><span class="line">				+ <span class="string">&quot;********************************************************************\n\n&quot;</span>);</span><br><span class="line">		result = <span class="keyword">new</span> DebugFilter(filterChainProxy);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">this</span>.postBuildAction.run();</span><br><span class="line">	<span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>securityFilterChainBuilders</code>通常由框架自动注入，<code>WebSecurityConfigurerAdapter.init(WebSecurity)</code>，在<code>WebSecurity.init</code>的时候执行</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">(WebSecurity web)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">	HttpSecurity http = getHttp();</span><br><span class="line">	<span class="comment">//初始化时注入</span></span><br><span class="line">	web.addSecurityFilterChainBuilder(http).postBuildAction(() -&gt; &#123;</span><br><span class="line">		FilterSecurityInterceptor securityInterceptor = http.getSharedObject(FilterSecurityInterceptor.class);</span><br><span class="line">		web.securityInterceptor(securityInterceptor);</span><br><span class="line">	&#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="HttpSecurity"><a href="#HttpSecurity" class="headerlink" title="HttpSecurity"></a>HttpSecurity</h3><p><code>HttpSecurity</code>继承了<code>AbstractConfiguredSecurityBuilder&lt;DefaultSecurityFilterChain, HttpSecurity&gt;</code>，<code>AbstractSecurityBuilder</code>，是一个<code>SecurityBuilder</code>，用于构建<code>DefaultSecurityFilterChain</code>，可以配置特定<code>HTTP</code>请求的web安全，多个<code>HttpSecurity</code>可以分别配置不同维度的安全设置，同样使用父类的模板方法执行构建过程。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">HttpSecurity</span> <span class="keyword">extends</span> <span class="title">AbstractConfiguredSecurityBuilder</span>&lt;<span class="title">DefaultSecurityFilterChain</span>, <span class="title">HttpSecurity</span>&gt;</span></span><br><span class="line"><span class="class">		<span class="keyword">implements</span> <span class="title">SecurityBuilder</span>&lt;<span class="title">DefaultSecurityFilterChain</span>&gt;, <span class="title">HttpSecurityBuilder</span>&lt;<span class="title">HttpSecurity</span>&gt; </span>&#123;</span><br><span class="line">	<span class="comment">//设置多个SecurityConfigurer通用的AuthenticationManager</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">beforeConfigure</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">		setSharedObject(AuthenticationManager.class, getAuthenticationRegistry().build());</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//生成针对特定HTTP请求的安全过滤器链</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="function"><span class="keyword">protected</span> DefaultSecurityFilterChain <span class="title">performBuild</span><span class="params">()</span> </span>&#123;</span><br><span class="line">		<span class="keyword">this</span>.filters.sort(<span class="keyword">this</span>.comparator);</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">new</span> DefaultSecurityFilterChain(<span class="keyword">this</span>.requestMatcher, <span class="keyword">this</span>.filters);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//用于匹配特定的请求，这些请求可以应用这里配置的安全设置</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> HttpSecurity <span class="title">requestMatcher</span><span class="params">(RequestMatcher requestMatcher)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">this</span>.requestMatcher = requestMatcher;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>HttpSecurity</code>可以直接增加过滤器配置，另外还有很多预设的方法配置，例如：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//使用链式方法配置</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> HttpBasicConfigurer&lt;HttpSecurity&gt; <span class="title">httpBasic</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">	<span class="keyword">return</span> getOrApply(<span class="keyword">new</span> HttpBasicConfigurer&lt;&gt;());</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//避免重复配置</span></span><br><span class="line"><span class="keyword">private</span> &lt;C extends SecurityConfigurerAdapter&lt;DefaultSecurityFilterChain, HttpSecurity&gt;&gt; <span class="function">C <span class="title">getOrApply</span><span class="params">(C configurer)</span></span></span><br><span class="line"><span class="function">		<span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">	C existingConfig = (C) getConfigurer(configurer.getClass());</span><br><span class="line">	<span class="keyword">if</span> (existingConfig != <span class="keyword">null</span>) &#123;</span><br><span class="line">		<span class="keyword">return</span> existingConfig;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> apply(configurer);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//预设部分属性，加入配置器的集合中，在构建阶段调用相应的策略方法</span></span><br><span class="line"><span class="keyword">public</span> &lt;C extends SecurityConfigurerAdapter&lt;O, B&gt;&gt; <span class="function">C <span class="title">apply</span><span class="params">(C configurer)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">	configurer.addObjectPostProcessor(<span class="keyword">this</span>.objectPostProcessor);</span><br><span class="line">	configurer.setBuilder((B) <span class="keyword">this</span>);</span><br><span class="line">	add(configurer);</span><br><span class="line">	<span class="keyword">return</span> configurer;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//使用接口配置</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> HttpSecurity <span class="title">httpBasic</span><span class="params">(Customizer&lt;HttpBasicConfigurer&lt;HttpSecurity&gt;&gt; httpBasicCustomizer)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">	httpBasicCustomizer.customize(getOrApply(<span class="keyword">new</span> HttpBasicConfigurer&lt;&gt;()));</span><br><span class="line">	<span class="keyword">return</span> HttpSecurity.<span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//直接配置过滤器</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> HttpSecurity <span class="title">addFilter</span><span class="params">(Filter filter)</span> </span>&#123;</span><br><span class="line">	Class&lt;? extends Filter&gt; filterClass = filter.getClass();</span><br><span class="line">	<span class="keyword">if</span> (!<span class="keyword">this</span>.comparator.isRegistered(filterClass)) &#123;</span><br><span class="line">		<span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException(<span class="string">&quot;The Filter class &quot;</span> + filterClass.getName()</span><br><span class="line">				+ <span class="string">&quot; does not have a registered order and cannot be added without a specified order. Consider using addFilterBefore or addFilterAfter instead.&quot;</span>);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">this</span>.filters.add(filter);</span><br><span class="line">	<span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>以<code>HttpBasicConfigurer</code>为例</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">HttpBasicConfigurer</span>&lt;<span class="title">B</span> <span class="keyword">extends</span> <span class="title">HttpSecurityBuilder</span>&lt;<span class="title">B</span>&gt;&gt;</span></span><br><span class="line"><span class="class">		<span class="keyword">extends</span> <span class="title">AbstractHttpConfigurer</span>&lt;<span class="title">HttpBasicConfigurer</span>&lt;<span class="title">B</span>&gt;, <span class="title">B</span>&gt; </span>&#123;</span><br><span class="line">	<span class="comment">//模板方法 init 阶段执行策略函数，可对 HttpSecurity 进行配置</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">(B http)</span> </span>&#123;</span><br><span class="line">		registerDefaults(http);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//模板方法 configure 阶段执行，对 HttpSecurity 加入配置完成的 filter</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">configure</span><span class="params">(B http)</span> </span>&#123;</span><br><span class="line">		AuthenticationManager authenticationManager = http.getSharedObject(AuthenticationManager.class);</span><br><span class="line">		BasicAuthenticationFilter basicAuthenticationFilter = <span class="keyword">new</span> BasicAuthenticationFilter(authenticationManager,</span><br><span class="line">				<span class="keyword">this</span>.authenticationEntryPoint);</span><br><span class="line">		<span class="keyword">if</span> (<span class="keyword">this</span>.authenticationDetailsSource != <span class="keyword">null</span>) &#123;</span><br><span class="line">			basicAuthenticationFilter.setAuthenticationDetailsSource(<span class="keyword">this</span>.authenticationDetailsSource);</span><br><span class="line">		&#125;</span><br><span class="line">		RememberMeServices rememberMeServices = http.getSharedObject(RememberMeServices.class);</span><br><span class="line">		<span class="keyword">if</span> (rememberMeServices != <span class="keyword">null</span>) &#123;</span><br><span class="line">			basicAuthenticationFilter.setRememberMeServices(rememberMeServices);</span><br><span class="line">		&#125;</span><br><span class="line">		basicAuthenticationFilter = postProcess(basicAuthenticationFilter);</span><br><span class="line">		http.addFilter(basicAuthenticationFilter);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//继承 SecurityConfigurerAdapter ，返回构建器，用以支持链式方法调用</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> B <span class="title">and</span><span class="params">()</span> </span>&#123;</span><br><span class="line">		<span class="keyword">return</span> getBuilder();</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>另外还有<code>openidLogin</code>，<code>headers</code>，<code>cors</code>，<code>sessionManagement</code>，<code>csrf</code>等便捷方法</p>
<h3 id="WebSecurityConfigurerAdapter"><a href="#WebSecurityConfigurerAdapter" class="headerlink" title="WebSecurityConfigurerAdapter"></a>WebSecurityConfigurerAdapter</h3><p>通过<code>WebSecurityConfigurerAdapter</code>可以方便的配置<code>HttpSecurity</code>，<code>AuthenticationManagerBuilder</code>的构建过程</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//配置HttpSecurity的构建</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">configure</span><span class="params">(HttpSecurity http)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">	http</span><br><span class="line">		.authorizeRequests(authorize -&gt; authorize</span><br><span class="line">			.anyRequest().authenticated()</span><br><span class="line">		)</span><br><span class="line">		.formLogin(withDefaults())</span><br><span class="line">		.httpBasic(withDefaults());</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//配置AuthenticationManager的构建</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">configure</span><span class="params">(AuthenticationManagerBuilder auth)</span> </span>&#123;</span><br><span class="line"><span class="comment">// enable in memory based authentication with a user named</span></span><br><span class="line"><span class="comment">// &quot;user&quot; and &quot;admin&quot;</span></span><br><span class="line">	auth</span><br><span class="line">	.inMemoryAuthentication().withUser(<span class="string">&quot;user&quot;</span>).password(<span class="string">&quot;password&quot;</span>).roles(<span class="string">&quot;USER&quot;</span>).and()</span><br><span class="line">		.withUser(<span class="string">&quot;admin&quot;</span>).password(<span class="string">&quot;password&quot;</span>).roles(<span class="string">&quot;USER&quot;</span>, <span class="string">&quot;ADMIN&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Expose the UserDetailsService as a Bean</span></span><br><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> UserDetailsService <span class="title">userDetailsServiceBean</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line"><span class="keyword">return</span> <span class="keyword">super</span>.userDetailsServiceBean();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>初始化<code>HttpSecurity</code>，构造需要三个参数</p>
<p><code>ObjectPostProcessor</code>，用于支持<code>Spring</code>的一些周期函数</p>
<p><code>AuthenticationManagerBuilder</code>，用于构建<code>AuthenticationManager</code>，认证工作都由它来完成</p>
<p><code>sharedObjects</code>，在此构建器的多个<code>Configurer</code>中共享的对象</p>
<p>实例化之后，初始化默认配置，应用子类配置</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">final</span> HttpSecurity <span class="title">getHttp</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">	<span class="keyword">if</span> (<span class="keyword">this</span>.http != <span class="keyword">null</span>) &#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">this</span>.http;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//认证事件发布器</span></span><br><span class="line">	AuthenticationEventPublisher eventPublisher = getAuthenticationEventPublisher();</span><br><span class="line">	<span class="comment">//localConfigureAuthenticationBldr是本地的AuthenticationManager构建器</span></span><br><span class="line">	<span class="keyword">this</span>.localConfigureAuthenticationBldr.authenticationEventPublisher(eventPublisher);</span><br><span class="line">	<span class="comment">//为每个filter的构建器共享的认证方案，可以根据子类自定义配置构建，子类没有重写使用默认配置</span></span><br><span class="line">	AuthenticationManager authenticationManager = authenticationManager();</span><br><span class="line">	<span class="comment">//认证管理器构建器，每个filter拥有一个局部的认证管理器</span></span><br><span class="line">	<span class="keyword">this</span>.authenticationBuilder.parentAuthenticationManager(authenticationManager);</span><br><span class="line">	<span class="comment">//为每个filter的构建器共享的对象</span></span><br><span class="line">	Map&lt;Class&lt;?&gt;, Object&gt; sharedObjects = createSharedObjects();</span><br><span class="line">	<span class="keyword">this</span>.http = <span class="keyword">new</span> HttpSecurity(<span class="keyword">this</span>.objectPostProcessor, <span class="keyword">this</span>.authenticationBuilder, sharedObjects);</span><br><span class="line">	<span class="comment">//可以通过设置disableDefaults关闭默认配置</span></span><br><span class="line">	<span class="keyword">if</span> (!<span class="keyword">this</span>.disableDefaults) &#123;</span><br><span class="line">		applyDefaultConfiguration(<span class="keyword">this</span>.http);</span><br><span class="line">		ClassLoader classLoader = <span class="keyword">this</span>.context.getClassLoader();</span><br><span class="line">		<span class="comment">//spring的SPI机制加载默认配置</span></span><br><span class="line">		List&lt;AbstractHttpConfigurer&gt; defaultHttpConfigurers = SpringFactoriesLoader</span><br><span class="line">				.loadFactories(AbstractHttpConfigurer.class, classLoader);</span><br><span class="line">		<span class="keyword">for</span> (AbstractHttpConfigurer configurer : defaultHttpConfigurers) &#123;</span><br><span class="line">			<span class="keyword">this</span>.http.apply(configurer);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//回调函数修改默认配置</span></span><br><span class="line">	configure(<span class="keyword">this</span>.http);</span><br><span class="line">	<span class="keyword">return</span> <span class="keyword">this</span>.http;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//应用默认的配置</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">applyDefaultConfiguration</span><span class="params">(HttpSecurity http)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">	http.csrf();</span><br><span class="line">	http.addFilter(<span class="keyword">new</span> WebAsyncManagerIntegrationFilter());</span><br><span class="line">	http.exceptionHandling();</span><br><span class="line">	http.headers();</span><br><span class="line">	http.sessionManagement();</span><br><span class="line">	http.securityContext();</span><br><span class="line">	http.requestCache();</span><br><span class="line">	http.anonymous();</span><br><span class="line">	http.servletApi();</span><br><span class="line">	http.apply(<span class="keyword">new</span> DefaultLoginPageConfigurer&lt;&gt;());</span><br><span class="line">	http.logout();</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//共享对象</span></span><br><span class="line"><span class="keyword">private</span> Map&lt;Class&lt;?&gt;, Object&gt; createSharedObjects() &#123;</span><br><span class="line">	Map&lt;Class&lt;?&gt;, Object&gt; sharedObjects = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line">	sharedObjects.putAll(<span class="keyword">this</span>.localConfigureAuthenticationBldr.getSharedObjects());</span><br><span class="line">	sharedObjects.put(UserDetailsService.class, userDetailsService());</span><br><span class="line">	sharedObjects.put(ApplicationContext.class, <span class="keyword">this</span>.context);</span><br><span class="line">	sharedObjects.put(ContentNegotiationStrategy.class, <span class="keyword">this</span>.contentNegotiationStrategy);</span><br><span class="line">	sharedObjects.put(AuthenticationTrustResolver.class, <span class="keyword">this</span>.trustResolver);</span><br><span class="line">	<span class="keyword">return</span> sharedObjects;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">protected</span> AuthenticationManager <span class="title">authenticationManager</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">	<span class="comment">//只需要初始化一次</span></span><br><span class="line">	<span class="keyword">if</span> (!<span class="keyword">this</span>.authenticationManagerInitialized) &#123;</span><br><span class="line">		<span class="comment">//设置disableLocalConfigureAuthenticationBldr为true，可在子类重写</span></span><br><span class="line">		configure(<span class="keyword">this</span>.localConfigureAuthenticationBldr);</span><br><span class="line">		<span class="keyword">if</span> (<span class="keyword">this</span>.disableLocalConfigureAuthenticationBldr) &#123;</span><br><span class="line">			<span class="comment">//配置类中获取全局默认配置authenticationManager</span></span><br><span class="line">			<span class="keyword">this</span>.authenticationManager = <span class="keyword">this</span>.authenticationConfiguration.getAuthenticationManager();</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span> &#123;</span><br><span class="line">			<span class="comment">//使用本地的构建器构建</span></span><br><span class="line">			<span class="keyword">this</span>.authenticationManager = <span class="keyword">this</span>.localConfigureAuthenticationBldr.build();</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">this</span>.authenticationManagerInitialized = <span class="keyword">true</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="keyword">this</span>.authenticationManager;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//AuthenticationManager的构建器使用DefaultPasswordEncoderAuthenticationManagerBuilder</span></span><br><span class="line"><span class="meta">@Autowired</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setApplicationContext</span><span class="params">(ApplicationContext context)</span> </span>&#123;</span><br><span class="line">	<span class="keyword">this</span>.context = context;</span><br><span class="line">	ObjectPostProcessor&lt;Object&gt; objectPostProcessor = context.getBean(ObjectPostProcessor.class);</span><br><span class="line">	LazyPasswordEncoder passwordEncoder = <span class="keyword">new</span> LazyPasswordEncoder(context);</span><br><span class="line">	<span class="keyword">this</span>.authenticationBuilder = <span class="keyword">new</span> DefaultPasswordEncoderAuthenticationManagerBuilder(objectPostProcessor,</span><br><span class="line">			passwordEncoder);</span><br><span class="line">	<span class="keyword">this</span>.localConfigureAuthenticationBldr = <span class="keyword">new</span> DefaultPasswordEncoderAuthenticationManagerBuilder(</span><br><span class="line">			objectPostProcessor, passwordEncoder) &#123;</span><br><span class="line"></span><br><span class="line">		<span class="meta">@Override</span></span><br><span class="line">		<span class="function"><span class="keyword">public</span> AuthenticationManagerBuilder <span class="title">eraseCredentials</span><span class="params">(<span class="keyword">boolean</span> eraseCredentials)</span> </span>&#123;</span><br><span class="line">			WebSecurityConfigurerAdapter.<span class="keyword">this</span>.authenticationBuilder.eraseCredentials(eraseCredentials);</span><br><span class="line">			<span class="keyword">return</span> <span class="keyword">super</span>.eraseCredentials(eraseCredentials);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="meta">@Override</span></span><br><span class="line">		<span class="function"><span class="keyword">public</span> AuthenticationManagerBuilder <span class="title">authenticationEventPublisher</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">				AuthenticationEventPublisher eventPublisher)</span> </span>&#123;</span><br><span class="line">			WebSecurityConfigurerAdapter.<span class="keyword">this</span>.authenticationBuilder.authenticationEventPublisher(eventPublisher);</span><br><span class="line">			<span class="keyword">return</span> <span class="keyword">super</span>.authenticationEventPublisher(eventPublisher);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">	&#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>HttpSecurity</code>可以使用<code>formLogin</code>，<code>httpBasic</code>等方法配置认证方式</p>
<p>通过<code>AuthenticationManagerBuilder</code>配置<code>AuthenticationProvider</code>支持具体的认证逻辑</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">HttpSecurity</span><span class="params">(ObjectPostProcessor&lt;Object&gt; objectPostProcessor,</span></span></span><br><span class="line"><span class="function"><span class="params">		AuthenticationManagerBuilder authenticationBuilder, Map&lt;Class&lt;?&gt;, Object&gt; sharedObjects)</span> </span>&#123;</span><br><span class="line">	<span class="keyword">super</span>(objectPostProcessor);</span><br><span class="line">	Assert.notNull(authenticationBuilder, <span class="string">&quot;authenticationBuilder cannot be null&quot;</span>);</span><br><span class="line">	<span class="comment">//authenticationBuilder加入到共享对象，通过getAuthenticationRegistry方法取出</span></span><br><span class="line">	setSharedObject(AuthenticationManagerBuilder.class, authenticationBuilder);</span><br><span class="line">	<span class="keyword">for</span> (Map.Entry&lt;Class&lt;?&gt;, Object&gt; entry : sharedObjects.entrySet()) &#123;</span><br><span class="line">		setSharedObject((Class&lt;Object&gt;) entry.getKey(), entry.getValue());</span><br><span class="line">	&#125;</span><br><span class="line">	ApplicationContext context = (ApplicationContext) sharedObjects.get(ApplicationContext.class);</span><br><span class="line">	<span class="keyword">this</span>.requestMatcherConfigurer = <span class="keyword">new</span> RequestMatcherConfigurer(context);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//表单认证</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> FormLoginConfigurer&lt;HttpSecurity&gt; <span class="title">formLogin</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">	<span class="keyword">return</span> getOrApply(<span class="keyword">new</span> FormLoginConfigurer&lt;&gt;());</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//http basic头认证</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> HttpBasicConfigurer&lt;HttpSecurity&gt; <span class="title">httpBasic</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">	<span class="keyword">return</span> getOrApply(<span class="keyword">new</span> HttpBasicConfigurer&lt;&gt;());</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//取出认证管理器构建器</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> AuthenticationManagerBuilder <span class="title">getAuthenticationRegistry</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	<span class="keyword">return</span> getSharedObject(AuthenticationManagerBuilder.class);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//配置管理器构建器增加provider</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> HttpSecurity <span class="title">authenticationProvider</span><span class="params">(AuthenticationProvider authenticationProvider)</span> </span>&#123;</span><br><span class="line">	getAuthenticationRegistry().authenticationProvider(authenticationProvider);</span><br><span class="line">	<span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//配置管理器构建器增加provider</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> HttpSecurity <span class="title">userDetailsService</span><span class="params">(UserDetailsService userDetailsService)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">	getAuthenticationRegistry().userDetailsService(userDetailsService);</span><br><span class="line">	<span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//在周期函数中完成认证管理器的构建</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">beforeConfigure</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">	setSharedObject(AuthenticationManager.class, getAuthenticationRegistry().build());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>HttpSecurity</code>通过<code>authorizeRequests</code>方法控制需要保护的资源，通过url匹配的方式</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">configure</span><span class="params">(HttpSecurity http)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">	http.authorizeRequests()</span><br><span class="line">		<span class="comment">//url /admin/** 需要角色ADMIN</span></span><br><span class="line">		.antMatchers(<span class="string">&quot;/admin/**&quot;</span>).hasRole(<span class="string">&quot;ADMIN&quot;</span>)</span><br><span class="line">		.antMatchers(<span class="string">&quot;/**&quot;</span>).hasRole(<span class="string">&quot;USER&quot;</span>)</span><br><span class="line">		.and().formLogin();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="ExpressionUrlAuthorizationConfigurer"><a href="#ExpressionUrlAuthorizationConfigurer" class="headerlink" title="ExpressionUrlAuthorizationConfigurer"></a>ExpressionUrlAuthorizationConfigurer</h3><p><code>authorizeRequests</code>通过<code>ExpressionUrlAuthorizationConfigurer</code>类配置</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> ExpressionUrlAuthorizationConfigurer&lt;HttpSecurity&gt;.<span class="function">ExpressionInterceptUrlRegistry <span class="title">authorizeRequests</span><span class="params">()</span></span></span><br><span class="line"><span class="function">		<span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">	ApplicationContext context = getContext();</span><br><span class="line">	<span class="keyword">return</span> getOrApply(<span class="keyword">new</span> ExpressionUrlAuthorizationConfigurer&lt;&gt;(context)).getRegistry();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>ExpressionUrlAuthorizationConfigurer</code>用于构建<code>FilterSecurityInterceptor</code>，以过滤器的形式控制资源访问</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">ExpressionUrlAuthorizationConfigurer</span>&lt;<span class="title">H</span> <span class="keyword">extends</span> <span class="title">HttpSecurityBuilder</span>&lt;<span class="title">H</span>&gt;&gt;</span></span><br><span class="line"><span class="class">		<span class="keyword">extends</span> <span class="title">AbstractInterceptUrlConfigurer</span>&lt;<span class="title">ExpressionUrlAuthorizationConfigurer</span>&lt;<span class="title">H</span>&gt;, <span class="title">H</span>&gt; </span>&#123;</span><br><span class="line">	<span class="comment">//初始化一个url与访问权限的注册器</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="title">ExpressionUrlAuthorizationConfigurer</span><span class="params">(ApplicationContext context)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">this</span>.REGISTRY = <span class="keyword">new</span> ExpressionInterceptUrlRegistry(context);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//注册 url 与 访问权限 的映射关系</span></span><br><span class="line">	<span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">interceptUrl</span><span class="params">(Iterable&lt;? extends RequestMatcher&gt; requestMatchers,</span></span></span><br><span class="line"><span class="function"><span class="params">			Collection&lt;ConfigAttribute&gt; configAttributes)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">for</span> (RequestMatcher requestMatcher : requestMatchers) &#123;</span><br><span class="line">			<span class="keyword">this</span>.REGISTRY.addMapping(</span><br><span class="line">					<span class="keyword">new</span> AbstractConfigAttributeRequestMatcherRegistry.UrlMapping(requestMatcher, configAttributes));</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">//继承自AbstractInterceptUrlConfigurer</span></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">configure</span><span class="params">(H http)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">		<span class="comment">//封装url与访问权限等元数据</span></span><br><span class="line">		FilterInvocationSecurityMetadataSource metadataSource = createMetadataSource(http);</span><br><span class="line">		<span class="keyword">if</span> (metadataSource == <span class="keyword">null</span>) &#123;</span><br><span class="line">			<span class="keyword">return</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">//创建FilterSecurityInterceptor</span></span><br><span class="line">		FilterSecurityInterceptor securityInterceptor = createFilterSecurityInterceptor(http, metadataSource,</span><br><span class="line">				http.getSharedObject(AuthenticationManager.class));</span><br><span class="line">		<span class="keyword">if</span> (<span class="keyword">this</span>.filterSecurityInterceptorOncePerRequest != <span class="keyword">null</span>) &#123;</span><br><span class="line">			securityInterceptor.setObserveOncePerRequest(<span class="keyword">this</span>.filterSecurityInterceptorOncePerRequest);</span><br><span class="line">		&#125;</span><br><span class="line">		securityInterceptor = postProcess(securityInterceptor);</span><br><span class="line">		<span class="comment">//作为过滤器添加到HttpSecurity</span></span><br><span class="line">		http.addFilter(securityInterceptor);</span><br><span class="line">		<span class="comment">//作为共享对象</span></span><br><span class="line">		http.setSharedObject(FilterSecurityInterceptor.class, securityInterceptor);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//创建FilterSecurityInterceptor实例并赋值，初始化</span></span><br><span class="line">	<span class="function"><span class="keyword">private</span> FilterSecurityInterceptor <span class="title">createFilterSecurityInterceptor</span><span class="params">(H http,</span></span></span><br><span class="line"><span class="function"><span class="params">			FilterInvocationSecurityMetadataSource metadataSource, AuthenticationManager authenticationManager)</span></span></span><br><span class="line"><span class="function">			<span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">		FilterSecurityInterceptor securityInterceptor = <span class="keyword">new</span> FilterSecurityInterceptor();</span><br><span class="line">		securityInterceptor.setSecurityMetadataSource(metadataSource);</span><br><span class="line">		<span class="comment">//授权管理</span></span><br><span class="line">		securityInterceptor.setAccessDecisionManager(getAccessDecisionManager(http));</span><br><span class="line">		<span class="comment">//认证管理</span></span><br><span class="line">		securityInterceptor.setAuthenticationManager(authenticationManager);</span><br><span class="line">		securityInterceptor.afterPropertiesSet();</span><br><span class="line">		<span class="keyword">return</span> securityInterceptor;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="FilterSecurityInterceptor"><a href="#FilterSecurityInterceptor" class="headerlink" title="FilterSecurityInterceptor"></a>FilterSecurityInterceptor</h3><p><code>FilterSecurityInterceptor</code>通过过滤器的形式执行HTTP资源的安全处理，未经过认证的请求在这里拦截，配置拦截的url需要经过授权验证，符合权限的可以访问资源</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//过滤器方法</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doFilter</span><span class="params">(ServletRequest request, ServletResponse response, FilterChain chain)</span></span></span><br><span class="line"><span class="function">		<span class="keyword">throws</span> IOException, ServletException </span>&#123;</span><br><span class="line">	invoke(<span class="keyword">new</span> FilterInvocation(request, response, chain));</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//父类方法执行安全处理</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">invoke</span><span class="params">(FilterInvocation filterInvocation)</span> <span class="keyword">throws</span> IOException, ServletException </span>&#123;</span><br><span class="line">	<span class="keyword">if</span> (isApplied(filterInvocation) &amp;&amp; <span class="keyword">this</span>.observeOncePerRequest) &#123;</span><br><span class="line">		<span class="comment">// 已经处理过， 并且每个请求只验证一次 可以跳过验证</span></span><br><span class="line">		filterInvocation.getChain().doFilter(filterInvocation.getRequest(), filterInvocation.getResponse());</span><br><span class="line">		<span class="keyword">return</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// 标记为已处理</span></span><br><span class="line">	<span class="keyword">if</span> (filterInvocation.getRequest() != <span class="keyword">null</span> &amp;&amp; <span class="keyword">this</span>.observeOncePerRequest) &#123;</span><br><span class="line">		filterInvocation.getRequest().setAttribute(FILTER_APPLIED, Boolean.TRUE);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//前置处理</span></span><br><span class="line">	InterceptorStatusToken token = <span class="keyword">super</span>.beforeInvocation(filterInvocation);</span><br><span class="line">	<span class="keyword">try</span> &#123;</span><br><span class="line">		filterInvocation.getChain().doFilter(filterInvocation.getRequest(), filterInvocation.getResponse());</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">finally</span> &#123;</span><br><span class="line">		<span class="keyword">super</span>.finallyInvocation(token);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//后置处理</span></span><br><span class="line">	<span class="keyword">super</span>.afterInvocation(token, <span class="keyword">null</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>抽象安全拦截器，抽象了安全拦截管理的通用逻辑</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">AbstractSecurityInterceptor</span></span></span><br><span class="line"><span class="class">		<span class="keyword">implements</span> <span class="title">InitializingBean</span>, <span class="title">ApplicationEventPublisherAware</span>, <span class="title">MessageSourceAware</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="keyword">protected</span> InterceptorStatusToken <span class="title">beforeInvocation</span><span class="params">(Object object)</span> </span>&#123;</span><br><span class="line">		Assert.notNull(object, <span class="string">&quot;Object was null&quot;</span>);</span><br><span class="line">		<span class="keyword">if</span> (!getSecureObjectClass().isAssignableFrom(object.getClass())) &#123;</span><br><span class="line">			<span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException(<span class="string">&quot;Security invocation attempted for object &quot;</span> + object.getClass().getName()</span><br><span class="line">					+ <span class="string">&quot; but AbstractSecurityInterceptor only configured to support secure objects of type: &quot;</span></span><br><span class="line">					+ getSecureObjectClass());</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">//元数据中根据url匹配访问权限</span></span><br><span class="line">		Collection&lt;ConfigAttribute&gt; attributes = <span class="keyword">this</span>.obtainSecurityMetadataSource().getAttributes(object);</span><br><span class="line">		<span class="comment">//没有配置拦截的url直接放行</span></span><br><span class="line">		<span class="keyword">if</span> (CollectionUtils.isEmpty(attributes)) &#123;</span><br><span class="line">			Assert.isTrue(!<span class="keyword">this</span>.rejectPublicInvocations,</span><br><span class="line">					() -&gt; <span class="string">&quot;Secure object invocation &quot;</span> + object</span><br><span class="line">							+ <span class="string">&quot; was denied as public invocations are not allowed via this interceptor. &quot;</span></span><br><span class="line">							+ <span class="string">&quot;This indicates a configuration error because the &quot;</span></span><br><span class="line">							+ <span class="string">&quot;rejectPublicInvocations property is set to &#x27;true&#x27;&quot;</span>);</span><br><span class="line">			<span class="keyword">if</span> (<span class="keyword">this</span>.logger.isDebugEnabled()) &#123;</span><br><span class="line">				<span class="keyword">this</span>.logger.debug(LogMessage.format(<span class="string">&quot;Authorized public object %s&quot;</span>, object));</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="comment">//发布事件</span></span><br><span class="line">			publishEvent(<span class="keyword">new</span> PublicInvocationEvent(object));</span><br><span class="line">			<span class="keyword">return</span> <span class="keyword">null</span>; <span class="comment">// no further work post-invocation</span></span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">//未找到认证实体，发布事件 AuthenticationCredentialsNotFoundEvent，抛出异常 AuthenticationCredentialsNotFoundException</span></span><br><span class="line">		<span class="keyword">if</span> (SecurityContextHolder.getContext().getAuthentication() == <span class="keyword">null</span>) &#123;</span><br><span class="line">			credentialsNotFound(<span class="keyword">this</span>.messages.getMessage(<span class="string">&quot;AbstractSecurityInterceptor.authenticationNotFound&quot;</span>,</span><br><span class="line">					<span class="string">&quot;An Authentication object was not found in the SecurityContext&quot;</span>), object, attributes);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">//需要认证的调用authenticationManager进行认证</span></span><br><span class="line">		Authentication authenticated = authenticateIfRequired();</span><br><span class="line">		<span class="keyword">if</span> (<span class="keyword">this</span>.logger.isTraceEnabled()) &#123;</span><br><span class="line">			<span class="keyword">this</span>.logger.trace(LogMessage.format(<span class="string">&quot;Authorizing %s with attributes %s&quot;</span>, object, attributes));</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">// 尝试accessDecisionManager授权，没有通过权限认证会发布AuthorizationFailureEvent事件，抛出AccessDeniedException异常</span></span><br><span class="line">		attemptAuthorization(object, attributes, authenticated);</span><br><span class="line">		<span class="keyword">if</span> (<span class="keyword">this</span>.logger.isDebugEnabled()) &#123;</span><br><span class="line">			<span class="keyword">this</span>.logger.debug(LogMessage.format(<span class="string">&quot;Authorized %s with attributes %s&quot;</span>, object, attributes));</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">if</span> (<span class="keyword">this</span>.publishAuthorizationSuccess) &#123;</span><br><span class="line">			<span class="comment">//发布授权成功事件</span></span><br><span class="line">			publishEvent(<span class="keyword">new</span> AuthorizedEvent(object, attributes, authenticated));</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">//提供了一种机制，临时替换当前的认证信息，请求完成后重置，默认不做处理</span></span><br><span class="line">		Authentication runAs = <span class="keyword">this</span>.runAsManager.buildRunAs(authenticated, object, attributes);</span><br><span class="line">		<span class="keyword">if</span> (runAs != <span class="keyword">null</span>) &#123;</span><br><span class="line">			SecurityContext origCtx = SecurityContextHolder.getContext();</span><br><span class="line">			SecurityContextHolder.setContext(SecurityContextHolder.createEmptyContext());</span><br><span class="line">			SecurityContextHolder.getContext().setAuthentication(runAs);</span><br><span class="line"></span><br><span class="line">			<span class="keyword">if</span> (<span class="keyword">this</span>.logger.isDebugEnabled()) &#123;</span><br><span class="line">				<span class="keyword">this</span>.logger.debug(LogMessage.format(<span class="string">&quot;Switched to RunAs authentication %s&quot;</span>, runAs));</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="comment">// finallyInvocation中处理</span></span><br><span class="line">			<span class="keyword">return</span> <span class="keyword">new</span> InterceptorStatusToken(origCtx, <span class="keyword">true</span>, attributes, object);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">this</span>.logger.trace(<span class="string">&quot;Did not switch RunAs authentication since RunAsManager returned null&quot;</span>);</span><br><span class="line">		<span class="comment">// no further work post-invocation</span></span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">new</span> InterceptorStatusToken(SecurityContextHolder.getContext(), <span class="keyword">false</span>, attributes, object);</span><br><span class="line"></span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">private</span> Authentication <span class="title">authenticateIfRequired</span><span class="params">()</span> </span>&#123;</span><br><span class="line">		Authentication authentication = SecurityContextHolder.getContext().getAuthentication();</span><br><span class="line">		<span class="comment">//已经认证的 并且 总是重新验证 为 false 直接返回结果 </span></span><br><span class="line">		<span class="keyword">if</span> (authentication.isAuthenticated() &amp;&amp; !<span class="keyword">this</span>.alwaysReauthenticate) &#123;</span><br><span class="line">			<span class="keyword">if</span> (<span class="keyword">this</span>.logger.isTraceEnabled()) &#123;</span><br><span class="line">				<span class="keyword">this</span>.logger.trace(LogMessage.format(<span class="string">&quot;Did not re-authenticate %s before authorizing&quot;</span>, authentication));</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">return</span> authentication;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">//进行身份认证</span></span><br><span class="line">		authentication = <span class="keyword">this</span>.authenticationManager.authenticate(authentication);</span><br><span class="line">		<span class="comment">// Don&#x27;t authenticated.setAuthentication(true) because each provider does that</span></span><br><span class="line">		<span class="keyword">if</span> (<span class="keyword">this</span>.logger.isDebugEnabled()) &#123;</span><br><span class="line">			<span class="keyword">this</span>.logger.debug(LogMessage.format(<span class="string">&quot;Re-authenticated %s before authorizing&quot;</span>, authentication));</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">//保存重新的认证结果</span></span><br><span class="line">		SecurityContextHolder.getContext().setAuthentication(authentication);</span><br><span class="line">		<span class="keyword">return</span> authentication;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//临时存储的认证信息，在这里重置</span></span><br><span class="line">	<span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">finallyInvocation</span><span class="params">(InterceptorStatusToken token)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">if</span> (token != <span class="keyword">null</span> &amp;&amp; token.isContextHolderRefreshRequired()) &#123;</span><br><span class="line">			SecurityContextHolder.setContext(token.getSecurityContext());</span><br><span class="line">			<span class="keyword">if</span> (<span class="keyword">this</span>.logger.isDebugEnabled()) &#123;</span><br><span class="line">				<span class="keyword">this</span>.logger.debug(LogMessage.of(</span><br><span class="line">						() -&gt; <span class="string">&quot;Reverted to original authentication &quot;</span> + token.getSecurityContext().getAuthentication()));</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//后置处理 afterInvocationManager 可以在请求完成后 做一些事情</span></span><br><span class="line">	<span class="function"><span class="keyword">protected</span> Object <span class="title">afterInvocation</span><span class="params">(InterceptorStatusToken token, Object returnedObject)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">if</span> (token == <span class="keyword">null</span>) &#123;</span><br><span class="line">			<span class="comment">// public object</span></span><br><span class="line">			<span class="keyword">return</span> returnedObject;</span><br><span class="line">		&#125;</span><br><span class="line">		finallyInvocation(token); <span class="comment">// continue to clean in this method for passivity</span></span><br><span class="line">		<span class="keyword">if</span> (<span class="keyword">this</span>.afterInvocationManager != <span class="keyword">null</span>) &#123;</span><br><span class="line">			<span class="comment">// 执行后置处理，如果有的话</span></span><br><span class="line">			<span class="keyword">try</span> &#123;</span><br><span class="line">				returnedObject = <span class="keyword">this</span>.afterInvocationManager.decide(token.getSecurityContext().getAuthentication(),</span><br><span class="line">						token.getSecureObject(), token.getAttributes(), returnedObject);</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">catch</span> (AccessDeniedException ex) &#123;</span><br><span class="line">				publishEvent(<span class="keyword">new</span> AuthorizationFailureEvent(token.getSecureObject(), token.getAttributes(),</span><br><span class="line">						token.getSecurityContext().getAuthentication(), ex));</span><br><span class="line">				<span class="keyword">throw</span> ex;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> returnedObject;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="FormLoginConfigurer"><a href="#FormLoginConfigurer" class="headerlink" title="FormLoginConfigurer"></a>FormLoginConfigurer</h3><p>表单认证通过<code>FormLoginConfigurer</code>类配置</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> FormLoginConfigurer&lt;HttpSecurity&gt; <span class="title">formLogin</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">	<span class="keyword">return</span> getOrApply(<span class="keyword">new</span> FormLoginConfigurer&lt;&gt;());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>FormLoginConfigurer</code>用于构建<code>UsernamePasswordAuthenticationFilter</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">FormLoginConfigurer</span>&lt;<span class="title">H</span> <span class="keyword">extends</span> <span class="title">HttpSecurityBuilder</span>&lt;<span class="title">H</span>&gt;&gt; <span class="keyword">extends</span></span></span><br><span class="line"><span class="class">		<span class="title">AbstractAuthenticationFilterConfigurer</span>&lt;<span class="title">H</span>, <span class="title">FormLoginConfigurer</span>&lt;<span class="title">H</span>&gt;, <span class="title">UsernamePasswordAuthenticationFilter</span>&gt; </span>&#123;</span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="title">FormLoginConfigurer</span><span class="params">()</span> </span>&#123;</span><br><span class="line">		<span class="keyword">super</span>(<span class="keyword">new</span> UsernamePasswordAuthenticationFilter(), <span class="keyword">null</span>);</span><br><span class="line">		usernameParameter(<span class="string">&quot;username&quot;</span>);</span><br><span class="line">		passwordParameter(<span class="string">&quot;password&quot;</span>);</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>
<h3 id="UsernamePasswordAuthenticationFilter"><a href="#UsernamePasswordAuthenticationFilter" class="headerlink" title="UsernamePasswordAuthenticationFilter"></a>UsernamePasswordAuthenticationFilter</h3><p>继承<code>AbstractAuthenticationProcessingFilter</code>，负责从请求中解析认证信息并交给<code>AuthenticationManager</code>认证</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> Authentication <span class="title">attemptAuthentication</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span></span></span><br><span class="line"><span class="function">		<span class="keyword">throws</span> AuthenticationException </span>&#123;</span><br><span class="line">	<span class="keyword">if</span> (<span class="keyword">this</span>.postOnly &amp;&amp; !request.getMethod().equals(<span class="string">&quot;POST&quot;</span>)) &#123;</span><br><span class="line">		<span class="keyword">throw</span> <span class="keyword">new</span> AuthenticationServiceException(<span class="string">&quot;Authentication method not supported: &quot;</span> + request.getMethod());</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//从请求参数中获取用户名</span></span><br><span class="line">	String username = obtainUsername(request);</span><br><span class="line">	username = (username != <span class="keyword">null</span>) ? username : <span class="string">&quot;&quot;</span>;</span><br><span class="line">	username = username.trim();</span><br><span class="line">	<span class="comment">//从请求参数中获取密码</span></span><br><span class="line">	String password = obtainPassword(request);</span><br><span class="line">	password = (password != <span class="keyword">null</span>) ? password : <span class="string">&quot;&quot;</span>;</span><br><span class="line">	<span class="comment">//封装token</span></span><br><span class="line">	UsernamePasswordAuthenticationToken authRequest = <span class="keyword">new</span> UsernamePasswordAuthenticationToken(username, password);</span><br><span class="line">	<span class="comment">// Allow subclasses to set the &quot;details&quot; property</span></span><br><span class="line">	<span class="comment">//token中保存额外的信息，如remoteAddress，sessionId</span></span><br><span class="line">	setDetails(request, authRequest);</span><br><span class="line">	<span class="comment">//调用AuthenticationManager进行认证</span></span><br><span class="line">	<span class="keyword">return</span> <span class="keyword">this</span>.getAuthenticationManager().authenticate(authRequest);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="AbstractAuthenticationProcessingFilter-1"><a href="#AbstractAuthenticationProcessingFilter-1" class="headerlink" title="AbstractAuthenticationProcessingFilter"></a>AbstractAuthenticationProcessingFilter</h3><p>处理基于http请求的认证过滤器抽象类，使用模板方法，完成过滤器的工作，将认证工作委托给子类处理</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doFilter</span><span class="params">(ServletRequest request, ServletResponse response, FilterChain chain)</span></span></span><br><span class="line"><span class="function">		<span class="keyword">throws</span> IOException, ServletException </span>&#123;</span><br><span class="line">	<span class="comment">//类似于适配器的处理</span></span><br><span class="line">	doFilter((HttpServletRequest) request, (HttpServletResponse) response, chain);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">doFilter</span><span class="params">(HttpServletRequest request, HttpServletResponse response, FilterChain chain)</span></span></span><br><span class="line"><span class="function">		<span class="keyword">throws</span> IOException, ServletException </span>&#123;</span><br><span class="line">	<span class="comment">//未匹配的请求不做处理</span></span><br><span class="line">	<span class="keyword">if</span> (!requiresAuthentication(request, response)) &#123;</span><br><span class="line">		chain.doFilter(request, response);</span><br><span class="line">		<span class="keyword">return</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">try</span> &#123;</span><br><span class="line">		<span class="comment">//模板方法，子类认证</span></span><br><span class="line">		Authentication authenticationResult = attemptAuthentication(request, response);</span><br><span class="line">		<span class="keyword">if</span> (authenticationResult == <span class="keyword">null</span>) &#123;</span><br><span class="line">			<span class="comment">//认证信息为空代表无法处理</span></span><br><span class="line">			<span class="comment">// return immediately as subclass has indicated that it hasn&#x27;t completed</span></span><br><span class="line">			<span class="keyword">return</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">//session处理的策略接口</span></span><br><span class="line">		<span class="keyword">this</span>.sessionStrategy.onAuthentication(authenticationResult, request, response);</span><br><span class="line">		<span class="comment">// Authentication success</span></span><br><span class="line">		<span class="comment">//可配置过滤器链后处理</span></span><br><span class="line">		<span class="keyword">if</span> (<span class="keyword">this</span>.continueChainBeforeSuccessfulAuthentication) &#123;</span><br><span class="line">			chain.doFilter(request, response);</span><br><span class="line">		&#125;</span><br><span class="line">		successfulAuthentication(request, response, chain, authenticationResult);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">catch</span> (InternalAuthenticationServiceException failed) &#123;</span><br><span class="line">		<span class="keyword">this</span>.logger.error(<span class="string">&quot;An internal error occurred while trying to authenticate the user.&quot;</span>, failed);</span><br><span class="line">		unsuccessfulAuthentication(request, response, failed);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">catch</span> (AuthenticationException ex) &#123;</span><br><span class="line">		<span class="comment">// Authentication failed</span></span><br><span class="line">		unsuccessfulAuthentication(request, response, ex);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//成功处理方法</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">successfulAuthentication</span><span class="params">(HttpServletRequest request, HttpServletResponse response, FilterChain chain,</span></span></span><br><span class="line"><span class="function"><span class="params">		Authentication authResult)</span> <span class="keyword">throws</span> IOException, ServletException </span>&#123;</span><br><span class="line">	<span class="comment">//保存认证信息到SecurityContextHolder</span></span><br><span class="line">	SecurityContextHolder.getContext().setAuthentication(authResult);</span><br><span class="line">	<span class="keyword">if</span> (<span class="keyword">this</span>.logger.isDebugEnabled()) &#123;</span><br><span class="line">		<span class="keyword">this</span>.logger.debug(LogMessage.format(<span class="string">&quot;Set SecurityContextHolder to %s&quot;</span>, authResult));</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//记住我</span></span><br><span class="line">	<span class="keyword">this</span>.rememberMeServices.loginSuccess(request, response, authResult);</span><br><span class="line">	<span class="comment">//发布对应事件</span></span><br><span class="line">	<span class="keyword">if</span> (<span class="keyword">this</span>.eventPublisher != <span class="keyword">null</span>) &#123;</span><br><span class="line">		<span class="keyword">this</span>.eventPublisher.publishEvent(<span class="keyword">new</span> InteractiveAuthenticationSuccessEvent(authResult, <span class="keyword">this</span>.getClass()));</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//成功回调，比如跳转到之前访问的url，可自定义</span></span><br><span class="line">	<span class="keyword">this</span>.successHandler.onAuthenticationSuccess(request, response, authResult);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//失败处理方法</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">unsuccessfulAuthentication</span><span class="params">(HttpServletRequest request, HttpServletResponse response,</span></span></span><br><span class="line"><span class="function"><span class="params">		AuthenticationException failed)</span> <span class="keyword">throws</span> IOException, ServletException </span>&#123;</span><br><span class="line">	<span class="comment">//清除认证信息</span></span><br><span class="line">	SecurityContextHolder.clearContext();</span><br><span class="line">	<span class="keyword">this</span>.logger.trace(<span class="string">&quot;Failed to process authentication request&quot;</span>, failed);</span><br><span class="line">	<span class="keyword">this</span>.logger.trace(<span class="string">&quot;Cleared SecurityContextHolder&quot;</span>);</span><br><span class="line">	<span class="keyword">this</span>.logger.trace(<span class="string">&quot;Handling authentication failure&quot;</span>);</span><br><span class="line">	<span class="comment">//清除记住我</span></span><br><span class="line">	<span class="keyword">this</span>.rememberMeServices.loginFail(request, response);</span><br><span class="line">	<span class="comment">//失败回调，比如跳转到指定url，返回401等，可自定义</span></span><br><span class="line">	<span class="keyword">this</span>.failureHandler.onAuthenticationFailure(request, response, failed);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Java</category>
        <category>Spring</category>
        <category>Spring Security</category>
      </categories>
      <tags>
        <tag>Spring</tag>
        <tag>Security</tag>
      </tags>
  </entry>
</search>
